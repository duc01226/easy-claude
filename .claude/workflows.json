{
  "$schema": "./workflows.schema.json",
  "description": "Canonical workflow definitions for AI agent orchestration",
  "version": "2.4.0",
  "workflows": {
    "workflow-big-feature": {
      "description": "Research-driven feature development for large, complex, or ambiguous features in an existing project — includes idea refinement, market research, business evaluation, domain analysis, tech stack research, full implementation, and required near-end workflow-e2e verification",
      "name": "Big Feature (Research + Implement)",
      "parallelGroups": [
        {
          "id": "architecture-gates",
          "members": [
            "architecture-scalability-review",
            "architecture-rationale-review"
          ],
          "conditionalMembers": [],
          "barrier": true
        }
      ],
      "stepMeta": {
        "architecture-scalability-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "architecture-rationale-review": {
          "executionMode": "inline"
        },
        "scan --target=ui-system": {
          "executionMode": "inline"
        },
        "scan --target=backend-patterns": {
          "executionMode": "inline"
        },
        "scan --target=integration-tests": {
          "executionMode": "inline"
        },
        "scan --target=project-structure": {
          "executionMode": "inline"
        }
      },
      "preActions": {
        "injectContext": "LARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, require the complete large_idea_decomposition block in the owning artifacts and carry its stable slice IDs through downstream outputs. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nBIG FEATURE PROTOCOL (Research-Driven):\nFor large/ambiguous features in an existing codebase that need research before implementation.\n\nMANDATORY IMPORTANT MUST ATTENTION RULES:\n1. EVERY research stage requires AskUserQuestion validation before proceeding\n2. Save artifacts to plan directory at EVERY step\n3. Present 2-4 options for every major decision with confidence %\n4. New Tech/Lib Gate: evaluate top 3 alternatives before adding any new dependency\n\nSTEP SELECTION GATE:\nAfter workflow activation, auto-select the applicable steps and skip irrelevant conditional steps. Default step set:\n- [x] Discovery Interview (idea)\n- [x] Market Source Discovery (web-research) — discover + triage candidate market sources; this is source gathering, NOT the sized market\n- [x] Deep Research (deep-research)\n- [x] Market Analysis (market-analysis) — CONDITIONAL: produce the sized market (TAM/SAM/SOM), competitor matrix, trends, SWOT, and customer segments when the registry occurrence's applicability selects it. Business Evaluation consumes this evidence and MUST NOT re-derive market sizing. Follow the sequence occurrence's applicability as the only run/skip rule; if skipped, Business Evaluation marks its market figures N/A with the reason rather than inventing them.\n- [x] Business Evaluation (business-evaluation)\n- [x] Refine to PBI (refine)\n- [x] Spec Discovery (spec-discovery) — investigate existing Feature Specs + related code before domain modeling; surfaces related/overlapping/affected specs + gaps + the invariant landscape, then a scope-decision gate (NEW/EXTEND/SPLIT). CONDITIONAL: short-circuits on an empty corpus\n- [x] Domain Analysis & ERD (domain-analysis)\n- [x] Tech Stack Research (tech-stack-research)\n- [x] Architecture Scalability Review (architecture-scalability-review) — grade project architecture + scalability BEFORE feature build; runs in PARALLEL with the design-rationale why-review (ARCHITECTURE GATES PARALLEL PHASE)\n- [x] User Stories (story)\n- [x] Feature Spec Consolidation (spec) — folds story/pbi-mockup into the tech-free 8-section Feature Spec; these are INPUTS, not re-authored\n- [x] Test Specifications (spec [mode=tests])\n- [x] Test Spec Review (artifact-review --type=spec-tests)\n- [x] Spec Clarification (spec-clarify) — review the consolidated Feature Spec vs the discovered system, audit hypotheses/decisions, and confirm every non-obvious decision with the user (AskUserQuestion) before the second (implementation) plan\n- [x] Implementation Plan (plan)\n- [x] Plan Review (plan-review)\n- [x] Plan Validation (plan-validate)\n- [x] Design Rationale Review (why-review) — the post-domain-analysis and architecture-rationale occurrences; plan rationale is reviewed inside every plan-review round by its parallel why-review sub-agent, so no why-review step follows plan-validate\n- [x] Foundation Review (architecture-review-full) — CONDITIONAL: after /scaffold, grade the built foundation (architecture + scalability + production-readiness) and fix BLOCKED/WARN findings before /plan-execute; skip when /scaffold was skipped\n- [x] Foundation Reference Doc Refresh (scan --target=ui-system|backend-patterns|integration-tests|project-structure) — CONDITIONAL: after Foundation Review, before /plan-validate and /plan-execute, REFRESH the project-reference docs from the reviewed foundation + golden-path examples when /scaffold ran; skip all four with a cited reason when /scaffold was skipped; skip scan --target=ui-system when no UI/frontend foundation was scaffolded\n- [x] Implementation (plan-execute)\n- [x] Seed Test Data (seed-test-data) — seed QC happy-path data via application-layer commands after implementation\n- [x] Domain Entity Review — DELEGATED to the nested /workflow-review-changes occurrence (conditional on domain entity files changing)\n- [x] Integration Tests (integration-test)\n- [x] Review Changes (workflow-review-changes) — consolidated review + fix loop\n- [x] Near-end E2E verification (workflow-e2e --source=context) — REQUIRED after review; nested workflow owns configured E2E and default-on screenshot review, with evidence-backed N/A/ENVIRONMENT-BLOCKED when unavailable\n- [x] Tests (test)\n- [x] Documentation — DELEGATED to the nested /workflow-review-changes occurrence (docs-update)\n- [x] Summary (watzup)\n\nAuto-skip steps that are irrelevant to the prompt; mark skipped steps as completed with a short reason.\n\nARCHITECTURE GATES PARALLEL PHASE (`architecture-gates` all-return barrier: architecture-scalability-review + the design-rationale why-review that follows it):\n- Both gates read the SAME finished architecture-design artifacts and neither consumes the other's output. FIRST launch /architecture-scalability-review as a fresh read-only `architect` sub-agent (brief: the architecture-design, domain-analysis and tech-stack-research artifact paths; it writes its scorecard under tmp/reports/ and validates its own sub-80 findings), THEN immediately run /why-review INLINE in FULL mode over the architecture-design rationale — inline so its Trade-Off Interrogation Gate can reach the user.\n- Advance only after BOTH return. Then reconcile the two reports: a scalability risk or sub-80 grade touching a decision the why-review passed becomes a WARN carried into /scenario and PLAN₁; a why-review FAIL — or a user Trade-Off answer that changes an architecture decision — blocks /scenario until the architecture decision is revised, then re-run both gates on the revised design.\n\nPLAN PHASES (quick reference):\n- PLAN₁ (after architecture-design): High-level architecture plan. Scope: system design, component boundaries, data flow, tech choices. Based on: research findings + domain analysis.\n- PLAN₂ (after artifact-review --type=spec-tests): Sprint-ready implementation plan. Scope: concrete tasks, file changes, test infrastructure, phased steps. Based on: stories + test specs + dependency tables.\nThe two plans serve different purposes — PLAN₁ is strategic, PLAN₂ is tactical.\n\nSECOND PLANNING ROUND:\nAfter stories + reviews are complete, a second /plan + /plan-review cycle runs.\nThe first /plan (after architecture-design) is high-level architecture based on research + domain analysis.\nThe second /plan (after artifact-review --type=spec-tests) incorporates the concrete stories, test specifications, dependency tables, and refinement details into a sprint-ready implementation plan with phased steps.\nThis ensures the implementation plan reflects all discovered requirements, test strategy, and story dependencies.\n\nTEST SPECIFICATIONS (after artifact-review --type=story, BEFORE second plan):\nAfter stories are reviewed, write TDD specs (/spec [mode=tests]) based on story acceptance criteria.\nReview specs (/artifact-review --type=spec-tests) for coverage and correctness.\nThe second /plan then incorporates test strategy alongside implementation tasks.\n\nARCHITECTURE SCAFFOLDING (after second plan-review, CONDITIONAL):\nThe /scaffold step is CONDITIONAL — AI must first self-investigate for existing base abstractions.\nGrep for: abstract/base classes, generic interfaces, infrastructure abstractions (IRepository, IUnitOfWork), utility layers (Extensions, Helpers, Utils), frontend foundations (base component/service/store), DI registrations.\nIf existing scaffolding found → SKIP /scaffold step, mark completed.\nIf NO foundational abstractions found → PROCEED: create all base abstract classes, generic interfaces, infrastructure abstractions, and shared utilities with OOP/SOLID principles BEFORE any feature story implementation.\nAll infrastructure behind interfaces with at least one concrete implementation (Dependency Inversion).\nFor existing projects adding a new module, adapt scaffolding to extend existing base classes rather than creating duplicates.\nPOST-SCAFFOLD FOUNDATION REVIEW GATE (after /scaffold, before /plan-execute): run /architecture-review-full on the built foundation (architecture-review + architecture-scalability-review + production-readiness-review → one consolidated Architecture Health Report) and fix ALL BLOCKED/WARN findings BEFORE /plan-execute — features MUST build on a reviewed, verified foundation, never on an unreviewed skeleton. CONDITIONAL: skip this review when /scaffold was skipped (existing foundation reused — nothing new to review). Honors the scale-tier guard — it ADVISES right-sized structure, never forces heavyweight architecture onto a small T0/B0 project.\nFOUNDATION REFERENCE-DOC REFRESH (after /architecture-review-full, before /plan-validate and /plan-execute, CONDITIONAL): new foundations need project references BEFORE feature fan-out. When /scaffold ran, run /scan --target=ui-system, /scan --target=backend-patterns, /scan --target=integration-tests, and /scan --target=project-structure INLINE in that order — each scan fans out its own sub-agents from the orchestrator, so a scan is never dispatched as a sub-agent, and the four scans write disjoint reference docs. This is a brownfield REFRESH, not a regeneration: each scan reads the existing {REF_DOCS_ROOT} doc first and surgically adds what the reviewed-and-fixed foundation introduced (new base abstractions, golden-path examples, the isolated examples tree, a new module, UI foundation) — creating a doc only when it does not exist yet — while preserving unchanged sections and manual annotations, so /plan-execute and every later feature copy patterns the references actually describe. Skip /scan --target=ui-system when /scaffold created no UI/frontend foundation (log the reason); the other three apply whenever /scaffold ran. When /scaffold was skipped (existing foundation reused, nothing new to document), mark all four scan steps completed with a cited skip reason — the existing reference docs already describe the reused foundation. If /plan-validate or a later pre-implementation fix changes the foundation, re-run every /scan whose reference doc covers the changed area before /plan-execute.\nMANDATORY SPEC-DRIVEN BIG-FEATURE GATES:\n- Read {REF_DOCS_ROOT}/spec-principles.md before /story and /spec [mode=tests] to lock intent and non-negotiable invariants.\n- /spec [mode=tests] + /artifact-review --type=spec-tests MUST map each invariant to Section 8 TC IDs.\n- STATE MACHINE DATA ASSERT (MOST IMPORTANT MANDATORY ASSERT): for lifecycle/state-machine flows, tests MUST assert persisted state transitions and invalid-transition rejection.\n- Before /workflow-end, enforce three-way sync: spec docs ↔ TDD docs ↔ test code via /spec [mode=tests] + /artifact-review --type=spec-tests + /integration-test + /integration-test-review + /integration-test-verify + /spec [mode=sync] + /docs-update.\nSCALE-TECHNIQUE GATE (advisory): during the tech-stack-research, architecture-design, architecture-scalability-review, and architecture-review-full steps, apply SYNC:scale-technique-gate — derive the target scale tier from evidence (T0 internal / T1 <10k / T2 10k–1M / T3 millions+), judge which system-design techniques (rate limiting, caching, load balancing, queues, sharding, autoscaling, CI/CD, observability, DR, etc.) that tier WARRANTS, and record the Technique Applicability Matrix (each warranted technique judged PRESENT / MISSING-WARRANTED / N/A-by-scale / OVER-ENGINEERED). Advise on warranted-but-missing gaps AND advise AGAINST over-provisioning below tier (do NOT add Kubernetes/sharding/multi-region for a small system). production-readiness-review + performance-review run later inside workflow-review-changes. Advisory guidance only — it never changes a score or verdict. Full catalog: .claude/docs/scale-technique-catalog.md.\nSCENARIO-STRESS EVAL (advisory): complementing the scale-technique gate top-down, during the tech-stack-research, architecture-design, architecture-scalability-review, and architecture-review-full steps (production-readiness-review + performance-review run later inside workflow-review-changes; and whenever solution-architect authors resilience posture), apply SYNC:scenario-stress-eval — REUSE the scale tier already derived by SYNC:scale-technique-gate, derive the orthogonal business-criticality tier (B0 non-critical / B1 important / B2 business-critical / B3 mission-critical or regulated) from evidence, then stress-test the design against concrete scenarios (traffic spike, sustained + data-volume growth, dependency down/slow, node/zone/region loss, data loss/corruption, poison-message/retry storm, cascading/backpressure, cold start, clock skew/duplicate delivery). Record a Scenario Stress Matrix judging each IN-SCOPE scenario WITHSTANDS / DEGRADES-GRACEFULLY / FAILS-HARD / N/A-by-business / OVER-HARDENED with self-heal + trade-off notes. Apply the criticality-signal floor: regulated / PII / financial / health data, money movement, authentication/identity, or legal-compliance scope floors B at B2+ even absent SLA/SLO docs. Right-size in BOTH directions — advise on FAILS-HARD gaps the business warrants AND advise AGAINST OVER-HARDENED resilience a lean B0/B1 system does not need. Advisory guidance only — it never changes a score or verdict. Full catalog: .claude/docs/scenario-stress-catalog.md.\nBROWNFIELD DESIGN-PHASE DIVERGENCE (rationale): big-feature intentionally OMITS greenfield's design-phase security-review, performance-review, linter-setup, and harness-setup — an existing codebase already has that tooling, and the later workflow-review-changes batch runs security-review + performance-review + production-readiness-review on the actual change. Adding them at design time would re-run setup the brownfield project already has (anti-over-engineering).\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (spec→code→tests→review loop): §8 must derive universally-quantified Invariant/Property TCs (for-ALL-inputs rules + boundary counter-cases) for every [HARD] §4 rule and §5 invariant — not just example scenarios — and back them with property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on changed core-logic = a missing invariant → write the killing test), NOT line-coverage %. Every behavior-changing finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a code-only fix. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-big-feature/SKILL.md"
        ]
      },
      "sequence": [
        "idea",
        "web-research",
        "deep-research",
        {
          "id": "market-analysis",
          "skill": "market-analysis",
          "applicability": {
            "when": "The work has a commercial market and either this product's addressable market is not already sized or this feature changes that sizing.",
            "skipReason": "This scope has no commercial market to size (for example, an internal tool, migration, or infrastructure-only change), or this product's addressable market is already sized and unchanged by this feature."
          }
        },
        "business-evaluation",
        "spec-discovery",
        "domain-analysis",
        "why-review",
        "tech-stack-research",
        "architecture-design",
        "architecture-scalability-review",
        {
          "id": "architecture-rationale-review",
          "skill": "why-review"
        },
        "scenario",
        "plan",
        "plan-review",
        "refine",
        "artifact-review --type=pbi",
        "story",
        "artifact-review --type=story",
        "pbi-challenge",
        "dor-gate",
        "pbi-mockup",
        "spec",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec-clarify",
        "plan",
        "plan-review",
        "scaffold",
        "architecture-review-full",
        "scan --target=ui-system",
        "scan --target=backend-patterns",
        "scan --target=integration-tests",
        "scan --target=project-structure",
        "plan-validate",
        "plan-execute",
        "seed-test-data",
        "integration-test",
        "integration-test-verify",
        "spec [mode=sync]",
        "workflow-review-changes",
        {
          "id": "big-feature-e2e",
          "skill": "workflow-e2e",
          "args": "--source=context"
        },
        "test",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to implement a large, complex, or ambiguous feature that needs research, market analysis, business evaluation, domain modeling, or tech stack analysis before implementation. Big new module, major enhancement, cross-cutting capability, or feature where scope is unclear"
    },
    "workflow-bugfix": {
      "description": "Systematic debugging and fix workflow with end-to-start debugger trace before fix and an explicit-request-only near-end workflow-e2e handoff",
      "name": "Bug Fix",
      "preActions": {
        "injectContext": "BUG FIX PROTOCOL (TDD-FIRST):\nPROJECT CONTEXT: Apply the shared SDD Artifact Contract from shared/sdd-artifact-contract.md in the active skills root. Read docs/project-config.json and {REF_DOCS_ROOT}/docs-index-reference.md for project-specific conventions. Any supported AI tool may implement or review when this context is synced.\n1. Debug: /debug-investigate owns BOTH discovery and root cause here — this workflow has no separate /investigate step. Find the files related to the reported issue, establish current vs expected behavior and the unchanged behavior that must be preserved, then identify the root cause with evidence (file:line)\n   IMPORTANT: When analyzing 'unused' code during investigation:\n   - Follow Investigation Protocol (CLAUDE.md)\n   - Require grep evidence, confidence >=80%, cross-module/service checks (see docs/project-config.json → workflowPatterns.crossModuleValidation)\n   - Invoke the /investigate skill ad hoc for removal/refactoring decisions\n1b. END-TO-START DEBUGGER TRACE GATE: Start at the observed final symptom/output, identify the final reader, trace backward through storage/projection, writer, consumer/job, producer/origin, enumerate all feeder paths, and build a hypothesis matrix. BLOCKED until owning fix layer and forward convergence proof are written.\n4. Plan fix with minimal blast radius: /plan is the single planning step — this workflow runs NO /plan-review and NO /plan-validate, so the plan's own evidence bar (root-cause trace, blast radius, rollback) must be met inside /plan before the fix\n6b. SPEC-BUG GATE — Run BEFORE writing regression TCs:\n   Ask: \"Is this a Code Bug or a Spec Bug?\"\n   • CODE BUG (code doesn't match spec — most common): Spec correctly describes expected behavior. Code diverged. Proceed to step 7.\n   • SPEC BUG (spec documented wrong behavior; code implemented the spec faithfully): Do NOT write regression TCs yet. First run /spec [mode=update] to correct the affected Feature Spec sections (§1-7, plus §8 if a TC encoded the wrong behavior). Then return to step 7.\n   • AMBIGUOUS: Ask user: \"Did the spec ever correctly document this behavior?\"\n   SIGNAL: Spec MATCHES buggy code → Spec Bug. Spec says X but code does Y → Code Bug.\n7. Write test specs (/spec [mode=tests]): Create TC specs asserting the CORRECT (fixed) expected behavior — not the buggy behavior. These become the regression guard.\n7b. UI-INTENT / INTERACTION-SURFACE REFRESH — CONDITIONAL: only when the fix changes user-facing behavior (else state the skip reason — backend-only fix, no §6 change). When user-facing behavior changed, run /spec (ui-intent intent) alongside the spec [mode=sync] reconciliation to refresh the affected Feature Spec §6 interaction surface — View Inventory, Key UI States, and the per-story click-path the bug touched — and link the governing design-spec so the §6 interaction-surface stays in sync with the corrected behavior.\n8. Review test specs with /artifact-review --type=spec-tests\n9. WRITE INTEGRATION TEST — RED phase: Implement integration test(s) based on the bug reproduction spec. Run the test(s) — they MUST FAIL. A passing test means it does NOT actually catch the bug. Never proceed to fix until the test(s) fail.\n10. Fix the identified issue\n11. RE-RUN INTEGRATION TESTS — GREEN phase: Run integration tests again — expect all to PASS. This confirms the fix resolves the bug AND regression guard is in place.\n12. Integration-test coverage review is owned by the nested /workflow-review-changes occurrence — it verifies tests have real assertion value, not just smoke/existence checks.\n13. Code review for quality and regression risk\n13b. Optional /workflow-e2e --source=context runs only after an explicit user E2E request; otherwise skip with the manifest applicability reason.\n14. Summary report of fix and verification results\n\nPERFORMANCE-SDD ROUTE: If this bug fix is performance-related (latency, throughput, memory, query speed, load behavior), run /performance-review and require SLA/benchmark evidence: target metric, baseline, measurement command, and acceptable regression budget. Do not use performance scope to bypass functional no-regression checks: run the relevant functional checks when behavior can change. Update the affected Feature Spec ({SPEC_ROOT}/{Bucket}/) for changed SLA, performance constraints, or behavior boundaries.\nMANDATORY INVARIANT-PRESERVING BUGFIX LOOP:\n- Do not encode buggy behavior into specs/tests. Confirm intended invariant from spec docs first.\n- /spec [mode=tests] MUST capture preserved invariants and newly-fixed invariants explicitly.\n- STATE MACHINE DATA ASSERT (MOST IMPORTANT MANDATORY ASSERT): regression tests MUST assert entity state before/after transitions and invalid transition rejection.\n- RED/GREEN harness proof is mandatory: first /integration-test must fail on the bug, second /integration-test must pass after fix.\n- /workflow-end is BLOCKED until specs, TCs, and test code are synchronized via /spec [mode=tests] + /artifact-review --type=spec-tests + /integration-test + /integration-test-review + /integration-test-verify + /spec [mode=sync] + /docs-update. Performance-related work may delegate measurement to /performance-review, but spec/test/docs sync remains required whenever behavior, public contract, SLA, performance constraints, or docs/spec boundaries change.\n- Code-to-spec extraction is reference-only until accepted by the canonical spec owner.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (spec→fix→tests→review loop): the regression §8 TCs must include universally-quantified Invariant/Property TCs (for-ALL-inputs rules + boundary counter-cases) for every [HARD] §4 rule and §5 invariant the bug touched — not just the single reproduction example — backed by property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on the fixed core-logic = a missing invariant → write the killing test), NOT line-coverage %. Every behavior-changing finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a code-only patch. Re-review the whole package (spec + tests + fix, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-bugfix/SKILL.md"
        ]
      },
      "sequence": [
        "debug-investigate",
        "spec [mode=amend]",
        "plan",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "integration-test",
        "fix",
        "integration-test",
        "integration-test-verify",
        "spec [mode=sync]",
        "workflow-review-changes",
        {
          "id": "bugfix-e2e",
          "skill": "workflow-e2e",
          "args": "--source=context",
          "applicability": {
            "when": "The user explicitly requests E2E work, such as asking to include, write, call, run, or do E2E or end-to-end verification.",
            "skipReason": "No explicit E2E request was found in the user prompt; the optional E2E handoff is disabled by default."
          }
        },
        "demo-guide",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User reports a bug, error, crash, failure, regression, stale/incorrect final output, or something not working; wants to fix/debug/troubleshoot an issue with end-to-start trace"
    },
    "workflow-e2e": {
      "description": "Write, update, and verify E2E tests using the configured project runner and a bounded green fix/retest loop; visual evidence follows the request and project contract",
      "name": "E2E Testing",
      "preActions": {
        "injectContext": "E2E WORKFLOW (configured lifecycle):\nResolve --source={changes|recording|update-ui|prompt|context|whole} and --visual-review={true|false} from the invocation, request, and applicable project contract. There is no framework-wide screenshot default; project-required review gates remain in force. Follow .claude/skills/workflow-e2e/SKILL.md.\nEvery source uses investigate -> conditional authoring/update -> e2e-test-verify --fix-loop -> docs-update -> workflow-end -> watzup. For changes, recording, and update-ui, e2e-test prepares the artifact and hands its exact scope and traceability to the loop; for prompt, context, and whole, the loop selects or generates the required case. The loop owns the configured run, applicable evidence, report-only `/experience-review`, failure classification, owning-layer fixes, review, fresh bring-up, and same-scope reruns.\n- changes: map changed behavior to configured owner/case/variant/assertion evidence from specArtifacts; use TC and section 8 only when no native profile is configured.\n- recording: validate the recording, identify the app/feature, map configured case identity, follow the project's test organization, add outcome assertions, and hand off to the loop.\n- update-ui: map visual changes to the project's existing test organization and spec/profile; collect candidate evidence without changing accepted expectations; preserve explicit acceptance for any baseline update.\n- prompt|context|whole: resolve scope and select or generate cases from verified project evidence using configured identities and the project's test organization.\n- Browser/UI: use the configured runner's native waits or an evidenced project helper for observable readiness and outcomes. Apply actionDelayMs only when the project contract documents a presentation need, after the outcome is stable; never use a delay as readiness or settling.\n- Visual: when review is requested or required for an applicable surface, capture and read the evidence required by project config and uiStateCapture.mode; validated blocking findings enter the failure set and require an owning-layer fix plus a fresh same-scope rerun.\n- Evidence: classify SOURCE-WRONG, TEST-WRONG, TEST-NOT-OPTIMAL, ENVIRONMENT-BLOCKED, or AMBIGUOUS before edits; never weaken, skip, narrow, delete, silence, or auto-accept to obtain green.",
        "readFiles": [
          ".claude/skills/e2e-test/SKILL.md",
          ".claude/skills/e2e-test-verify/SKILL.md",
          ".claude/skills/experience-review/SKILL.md",
          ".claude/skills/playwright-cli/SKILL.md",
          ".claude/skills/workflow-e2e/SKILL.md",
          ".claude/docs/configuration/experience-verification.md"
        ]
      },
      "sequence": [
        "investigate",
        {
          "id": "e2e-author",
          "skill": "e2e-test",
          "applicability": {
            "when": "Resolved --source is changes, recording, or update-ui; write or update the E2E artifact and hand its exact scope to the convergence loop.",
            "skipReason": "Resolved --source is prompt, context, or whole; e2e-test-verify --fix-loop owns scenario selection or generation for that source."
          }
        },
        {
          "id": "e2e-converge",
          "skill": "e2e-test-verify",
          "args": "--fix-loop"
        },
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to write, update, run, verify, or fix E2E coverage using the project's configured runner from code/spec changes, a recording, a UI change, a feature/bugfix/journey prompt, current context, or whole-project intent"
    },
    "workflow-feature": {
      "description": "Full feature development workflow with search-first approach, planning, implementation, testing, documentation, and an explicit-request-only near-end workflow-e2e handoff",
      "name": "Feature Implementation",
      "preActions": {
        "injectContext": "LARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, require the complete large_idea_decomposition block in the owning artifacts and carry its stable slice IDs through downstream outputs. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nFEATURE IMPLEMENTATION PROTOCOL:\n⚠️ PROJECT CONTEXT: Read docs/project-config.json → workflowPatterns and {REF_DOCS_ROOT}/docs-index-reference.md for project-specific architecture, test, documentation, naming, and CSS conventions. Apply the shared SDD Artifact Contract from shared/sdd-artifact-contract.md in the active skills root. Any supported AI tool may implement or review when this context is synced.\n⚠️ MANDATORY: Search existing code BEFORE planning\n1. Investigate: Find similar features and patterns, then study existing implementations and validate with 3+ codebase examples (NOT generic framework docs)\n2a. Spec Discovery — CONDITIONAL (short-circuits on empty spec corpus): run /spec-discovery after investigate to investigate all existing Feature Specs AND related code logic FIRST — surfacing related/overlapping/affected specs, missing features/test cases, and the invariant landscape — before authoring the spec.\n2b. Domain Analysis — CONDITIONAL: if feature creates/modifies domain entities, run /domain-analysis after spec-discovery to model bounded contexts and ERD before planning.\n3. Author Feature Spec: with /spec BEFORE planning, capture intended behavior — §1-7 business rules, invariants, and acceptance criteria the plan and tests are built against. Validate investigation + spec rationale with /why-review.\n3b. Spec Clarification (spec-clarify) — BLOCKING AskUserQuestion gate, BEFORE any planning: validate the freshly-authored §1-7 Feature Spec against the discovered system (AUTHORED-SPEC context), classify each surfaced decision OBVIOUS / NON-OBVIOUS / CONFLICTS, and confirm every NON-OBVIOUS + CONFLICTS + high-impact decision with the user (AskUserQuestion, recommended option first) before /plan. Spec Validation: questions=5-10.\n4. Plan: Design solution following discovered project patterns (architecture, state management, CSS — see docs/project-config.json → workflowPatterns). Include expected behavior, unchanged behavior, and docs/spec/test sync when behavior can change.\n5. Validate plan via /plan-review (every round runs a parallel full-mode /why-review rationale sub-agent beside its core pass, then /why-review --validate-findings on the merged findings) then /plan-validate before any code changes.\n6. Write test specifications with /spec [mode=tests] (before implementation). Review with /artifact-review --type=spec-tests.\n7. Update plan with test strategy via /plan (re-plan cycle). Review with /plan-review.\n8. Implement with /plan-execute (backend + frontend) — guided by test specs\n8b. Domain Entity Review — DELEGATED: when domain entity files are created/modified, the DDD-quality gate runs inside the nested /workflow-review-changes occurrence; do not repeat it here.\n9. Update test specs to catch implementation gaps with /spec [mode=tests]. Review with /artifact-review --type=spec-tests. Sync §8 TCs ↔ integration test code with /spec [mode=sync].\n9b. UI-INTENT / INTERACTION-SURFACE REFRESH — CONDITIONAL: only when the feature adds or changes user-facing behavior (else state the skip reason — backend-only feature, no §6 change). When user-facing behavior is present, run /spec (ui-intent intent) alongside the spec [mode=sync] step to refresh the affected Feature Spec §6 interaction surface — View Inventory, Key UI States, and the per-story click-path — and link the governing /design-spec so the §6 interaction-surface and the design-spec stay coupled to what was actually built.\n10. Generate/update integration tests with /integration-test — creates actual test files from TC specifications — then run /integration-test-verify. The deeper /integration-test-review coverage gate is owned by the nested /workflow-review-changes occurrence.\n11. Review the full change set with /workflow-review-changes (simplification, code quality, UI, architecture, and patterns compliance).\n11b. Optional /workflow-e2e --source=context runs only after an explicit user E2E request; otherwise skip with the manifest applicability reason.\n12. Security review for production readiness is owned by the nested /workflow-review-changes occurrence — do not repeat it here.\n13. Run tests to verify no regressions\n14. Update documentation if feature impacts business docs\n15. Summary report of all changes (/workflow-end + /watzup)\n\nPLAN PHASES:\n- PLAN₁ (after spec-clarify): Feature design plan. Scope: architecture, file changes, implementation approach.\n- PLAN₂ (after artifact-review --type=spec-tests): Updated plan incorporating test strategy. Scope: refine PLAN₁ with test infrastructure, test data setup, spec coverage gaps.\n\nGUARDRAIL: Provide file:line evidence of pattern search in plan. Follow project conventions over generic docs.\n\nPERFORMANCE-SDD ROUTE: If this feature is a performance enhancement (latency, throughput, memory, query speed, load behavior), run /performance-review and require SLA/benchmark evidence: target metric, baseline, measurement command, and acceptable regression budget. Do NOT skip /plan-execute. If behavior can change, run /test and relevant functional no-regression checks. Update the affected Feature Spec ({SPEC_ROOT}/{Bucket}/) for changed SLA, performance constraints, or behavior boundaries.\nMANDATORY SPEC-DRIVEN + INVARIANT + TEST HARNESS LOOP:\n- Read {REF_DOCS_ROOT}/spec-principles.md before /plan and lock feature intent + non-negotiable invariants.\n- /spec [mode=tests] MUST map every invariant to TC IDs in §8 Test Specifications.\n- STATE MACHINE DATA ASSERT (MOST IMPORTANT MANDATORY ASSERT): for lifecycle behavior, tests MUST assert persisted entity state transitions and invalid-transition rejection.\n- /workflow-end is BLOCKED until Feature Spec §1-7, §8 TCs, and test code are synchronized via /spec [mode=tests] + /artifact-review --type=spec-tests + /integration-test + /integration-test-review + /integration-test-verify + /spec [mode=sync] + /docs-update. Performance-related work may delegate measurement to /performance-review, but spec/test/docs sync remains required whenever behavior, public contract, SLA, performance constraints, or docs/spec boundaries change.\n- POST-IMPLEMENTATION SPEC RE-VERIFY (MANDATORY): the /spec authored BEFORE /plan captured intended behavior; after /plan-execute the implemented behavior may have diverged. Before closure, re-verify Feature Spec §1-7 against what was actually built and adjudicate any divergence per SYNC:spec-drift-adjudication (shared/sdd-artifact-contract.md Drift Gates) — CODE-WRONG -> fix code; SPEC-STALE -> run /spec [mode=update] to record the new intended behavior. This is not optional cleanup: a feature that shipped behavior the spec does not describe leaves the spec stale.\n- If mismatch exists (spec vs code vs tests), run /spec [mode=update] + /spec [mode=tests] before closure.\n- Code-to-spec extraction is reference-only until accepted by the canonical spec owner.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (spec→code→tests→review loop): §8 must derive universally-quantified Invariant/Property TCs (for-ALL-inputs rules + boundary counter-cases) for every [HARD] §4 rule and §5 invariant — not just example scenarios — and back them with property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on changed core-logic = a missing invariant → write the killing test), NOT line-coverage %. Every behavior-changing finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a code-only fix. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-feature/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "spec-discovery",
        "domain-analysis",
        "why-review",
        "spec",
        "spec-clarify",
        "scenario",
        "plan",
        "plan-review",
        "plan-validate",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "plan",
        "plan-review",
        "plan-execute",
        "seed-test-data",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec [mode=sync]",
        "integration-test",
        "integration-test-verify",
        "workflow-review-changes",
        {
          "id": "feature-e2e",
          "skill": "workflow-e2e",
          "args": "--source=context",
          "applicability": {
            "when": "The user explicitly requests E2E work, such as asking to include, write, call, run, or do E2E or end-to-end verification.",
            "skipReason": "No explicit E2E request was found in the user prompt; the optional E2E handoff is disabled by default."
          }
        },
        "test",
        "demo-guide",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to implement a well-defined feature, add a component, build a capability, develop a module, implement/execute an existing plan, create a new API endpoint, or design an API contract, TDD/test-first development, spec-driven feature implementation with test specs written before code"
    },
    "workflow-feature-spec": {
      "description": "Create or update the canonical feature/spec artifact under the configured root and active artifact contract, preserving planning, test/evidence coverage, and docs/change review. Use portable README/eight-section/TC defaults only when neither a native profile nor local artifact contract applies.",
      "name": "Business Feature Documentation",
      "preActions": {
        "injectContext": "CANONICAL FEATURE/SPEC PROTOCOL:\nRead docs/project-config.json fields specRoots.business.path, workflowPatterns.featureDocTemplate, docsRoots.projectReference.path, and specArtifacts when declared. Read the configured template plus local spec-system-reference.md and spec-principles.md. Resolve canonical paths from the configured business root and template. Resolve section roles, identifiers, ownership, and test/evidence carriers from specArtifacts and local references; generate only project-declared derived outputs. Apply tech-agnostic prose only to intent roles that require it.\nPortable defaults apply only when no native profile or local artifact contract exists: {SPEC_ROOT}/{Bucket}/README.{Feature}.md, the tech-free eight-section form with tech-free Sections 1–7, the mandatory inline §5 Mermaid ERD, and §6.2–§6.5 interaction intent for UI features, plus TC-{FEATURE}-{NNN} cases with user-visible GIVEN/WHEN/THEN, Business Intent / Invariant Guarded, Evidence: [Source: namespace/service/id], CoveredBy, and applicable status.\nFor behavior changes, reconcile the configured case/evidence carrier and link cases to actual execution proof. Preserve planning, plan-review, plan-validate, artifact/test review, docs-update, and change-review gates in their existing order. Cross-reference parent/child artifacts when the active contract defines the relation; when neither a native profile nor local artifact contract defines parent/child linkage, cross-reference a parent feature for each sub-feature. Unknown mappings or missing required coverage are UNKNOWN/BLOCKED, never NOT-APPLICABLE or PASS; malformed/conflicting contracts block instead of silently falling back.\nOUTPUT: the complete canonical artifact at its configured canonical path using its configured template. Use the README and portable sections only when neither a native profile nor local artifact contract defines another format.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: name the protected business intent or invariant and ensure the test fails when it breaks.",
        "readFiles": [
          ".claude/skills/workflow-feature-spec/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "plan",
        "plan-review",
        "plan-validate",
        "docs-update",
        "workflow-review-changes",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to create or update the project canonical feature/spec artifact under its configured root and active artifact contract."
    },
    "workflow-greenfield-init": {
      "description": "Full waterfall project inception from idea through implementation with integration testing and required near-end workflow-e2e verification",
      "name": "Greenfield Project Init",
      "parallelGroups": [
        {
          "id": "architecture-gates",
          "members": [
            "architecture-scalability-review",
            "architecture-rationale-review"
          ],
          "conditionalMembers": [],
          "barrier": true
        },
        {
          "id": "architecture-risk-reviews",
          "members": [
            "architecture-security-review",
            "architecture-performance-review"
          ],
          "conditionalMembers": [],
          "barrier": true
        },
        {
          "id": "reference-docs-and-rationale",
          "members": [
            "scan --target=ui-system",
            "scan --target=backend-patterns",
            "scan --target=integration-tests",
            "scan --target=project-structure",
            "pre-coding-rationale-review"
          ],
          "conditionalMembers": [
            "scan --target=ui-system"
          ],
          "barrier": true
        }
      ],
      "stepMeta": {
        "architecture-scalability-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "architecture-rationale-review": {
          "executionMode": "inline"
        },
        "architecture-security-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "architecture-performance-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "scan --target=ui-system": {
          "executionMode": "inline"
        },
        "scan --target=backend-patterns": {
          "executionMode": "inline"
        },
        "scan --target=integration-tests": {
          "executionMode": "inline"
        },
        "scan --target=project-structure": {
          "executionMode": "inline"
        },
        "pre-coding-rationale-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        }
      },
      "preActions": {
        "injectContext": "LARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, require the complete large_idea_decomposition block in the owning artifacts and carry its stable slice IDs through downstream outputs. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nGREENFIELD PROJECT INCEPTION PROTOCOL:\nYou are acting as a Solution Architect for a brand-new project.\n\nMANDATORY IMPORTANT MUST ATTENTION RULES:\n1. EVERY stage requires AskUserQuestion validation before proceeding\n2. Save artifacts to plan directory at EVERY step\n3. All tech recommendations include confidence % and evidence\n4. Present 2-4 options for every major decision\n5. Delegate architecture decisions to solution-architect agent\n6. After workflow activation, auto-select applicable steps and skip irrelevant conditional steps\n7. NEVER ask tech stack upfront — business analysis first, tech stack research after domain analysis\n8. Domain analysis produces ERD + bounded contexts BEFORE tech stack research\n9. Tech stack research compares top 3 options per layer with detailed pros/cons\n\nTEST ARCHITECTURE & EXECUTION CONTRACT GATE (BLOCKING):\n- During /architecture-design, emit the Test Architecture & Execution Contract matrix before the first /plan completes. Each potentially applicable Unit, Integration/System, and E2E tier gets a row. Mark APPLICABLE only with evidence-backed runner/framework/configuration; otherwise mark N/A — <evidence>.\n- Before /plan-execute or any feature-implementation handoff, require every APPLICABLE row to contain owner, test root, fixture/data strategy, copy-ready full command, focused command, zero-match behavior, CI gate, simple/Windows entry point, and unique run/data identity. Missing any required field BLOCKS handoff; never substitute assumptions for missing evidence.\n- After /integration-test-verify, inspect docs/project-config.json → e2eTesting and matching runnable framework, entry points, and commands. Run /e2e-test only when configured; otherwise record N/A — <evidence> citing the configuration and repository scan. The following /test step runs final full/focused verification and reports exact results and exit status.\n\nSTEP SELECTION GATE:\nAfter workflow activation, auto-select the applicable steps and skip irrelevant conditional steps. Default step set:\n- [x] Discovery Interview (idea)\n- [x] Market Source Discovery (web-research) — discover + triage candidate market sources; this is source gathering, NOT the sized market\n- [x] Deep Research (deep-research)\n- [x] Market Analysis (market-analysis) — CONDITIONAL: produce the sized market (TAM/SAM/SOM), competitor matrix, trends, SWOT, and customer segments that Business Evaluation consumes as evidence; select it whenever Business Evaluation is selected, because that skill MUST NOT re-derive market sizing itself. SKIP only when the project has no commercial market to size (internal tool, migration, infrastructure-only) — log the reason, and Business Evaluation must then mark its market figures N/A rather than invent them\n- [x] Business Evaluation (business-evaluation)\n- [x] Refine to PBI (refine)\n- [ ] Spec Discovery (spec-discovery) — CONDITIONAL: investigate existing Feature Specs + related code before domain modeling; AUTO-SKIPS at greenfield init (no specs/code yet) — runs only when initializing into a repo that already has specs/code\n- [x] Domain Analysis & ERD (domain-analysis) — NEW\n- [x] Tech Stack Research (tech-stack-research) — NEW\n- [x] Implementation Plan (plan)\n- [x] Plan Validation (plan-validate)\n- [x] Test Strategy (spec [mode=tests]) — includes integration test strategy\n- [ ] Spec Clarification (spec-clarify) — CONDITIONAL: only if a discrete Feature Spec §1-7 is authored (standard greenfield folds the spec into stories + test specs, so this typically auto-skips)\n- [x] User Stories (story)\n- [x] Final Review (plan-review)\n- [x] Foundation Review (architecture-review-full) — post-scaffold gate: grade the built foundation (architecture + scalability + production-readiness) and fix BLOCKED/WARN findings BEFORE /plan-execute\n- [x] Reference Doc Set (scan --target=ui-system|backend-patterns|integration-tests|project-structure) — after Foundation Review, before /plan-execute: DERIVE the project-reference doc set from the reviewed foundation + golden-path examples; scaffold already seeded ui-review-principles.md. CONDITIONAL: skip scan --target=ui-system when no UI stack (log reason); the other three always apply\n- [x] E2E Evaluation (e2e-test) — CONDITIONAL: run immediately after integration-test-verify only when docs/project-config.json → e2eTesting has a runnable framework, entry point, and command; otherwise record explicit evidence-backed N/A\n- [x] Near-end E2E workflow handoff (workflow-e2e --source=context) — REQUIRED after workflow-review-changes; default-on screenshot review is owned by the nested workflow, with evidence-backed N/A/ENVIRONMENT-BLOCKED when unavailable\n\nAuto-skip steps that are irrelevant to the prompt; mark skipped steps as completed with a short reason.\n\nARCHITECTURE GATES PARALLEL PHASE (`architecture-gates` all-return barrier: architecture-scalability-review + the design-rationale why-review that follows it):\n- Both gates read the SAME finished architecture-design artifacts and neither consumes the other's output. FIRST launch /architecture-scalability-review as a fresh read-only `architect` sub-agent (brief: the architecture-design, domain-analysis and tech-stack-research artifact paths; it writes its scorecard under tmp/reports/ and validates its own sub-80 findings), THEN immediately run /why-review INLINE in FULL mode over the architecture-design rationale — inline so its Trade-Off Interrogation Gate can reach the user.\n- Advance only after BOTH return. Then reconcile the two reports: a scalability risk or sub-80 grade touching a decision the why-review passed becomes a WARN carried into /scenario and PLAN₁; a why-review FAIL — or a user Trade-Off answer that changes an architecture decision — blocks /scenario until the architecture decision is revised, then re-run both gates on the revised design.\n\nARCHITECTURE RISK REVIEWS PARALLEL PHASE (`architecture-risk-reviews` all-return barrier: occurrences `architecture-security-review` + `architecture-performance-review`, the /security-review --report-only + /performance-review --report-only that follow PLAN₁'s first /plan-review):\n- Both review the SAME reviewed PLAN₁ + architecture-design artifacts and neither consumes the other's output. Launch BOTH in ONE message as fresh read-only sub-agents — /security-review --report-only via `security-auditor`, /performance-review --report-only via `performance-optimizer` (brief: the PLAN₁, architecture-design, domain-analysis and tech-stack-research artifact paths). Each runs its skill's documented Report-Only Mode: it audits the architecture at design altitude, writes only its report under tmp/reports/, validates its own findings through its skill's findings-validation gate, and returns — no fix, no nested sub-agent, no user question, and no /scan or /project-init for reference docs that do not exist yet; neither edits PLAN₁.\n- Advance only after BOTH return. The per-stage AskUserQuestion validation for both reviews runs after the barrier; the following /plan-review is the SOLE writer of PLAN₁ — it folds in both validated reports and surfaces any security-vs-performance conflict or trade-off question to the user, never resolving it silently.\n\nPLAN PHASES (quick reference):\n- PLAN₁ (after architecture-design): High-level architecture plan. Scope: system design, layer boundaries, component responsibilities, tech choices. Followed by the parallel /security-review + /performance-review review of the architecture (ARCHITECTURE RISK REVIEWS PARALLEL PHASE), then a second /plan-review that folds both reports into PLAN₁.\n- PLAN₂ (after artifact-review --type=spec-tests): Sprint-ready implementation plan. Scope: concrete tasks, file changes, scaffolding needs, test infrastructure. Based on: stories + test specs from TDD-SPEC₁.\n- PLAN₃ (after TDD-SPEC₂ post-implementation): Integration test architecture plan. Scope: test file structure, test data setup, CI integration. Based on: implementation code + updated test specs.\nThe three plans serve progressively detailed purposes — architecture → implementation → test infrastructure.\n\nSECOND PLANNING ROUND:\nAfter stories + TDD specs are generated and reviewed, a second /plan + /plan-review cycle runs.\nThis second plan incorporates the concrete stories, test specs, and dependency tables into a detailed implementation plan.\nThe first plan is high-level architecture; the second plan is sprint-ready with phased implementation steps.\n\nARCHITECTURE SCAFFOLDING (after second plan-review, CONDITIONAL):\nThe /scaffold step is CONDITIONAL — AI must first self-investigate for existing base abstractions.\nGrep for: abstract/base classes, generic interfaces, infrastructure abstractions (IRepository, IUnitOfWork), utility layers (Extensions, Helpers, Utils), frontend foundations (base component/service/store), DI registrations.\nIf existing scaffolding found → SKIP /scaffold step, mark completed.\nIf NO foundational abstractions found → PROCEED: create all base abstract classes, generic interfaces, infrastructure abstractions, and shared utilities with OOP/SOLID principles BEFORE any feature story implementation.\nAll infrastructure behind interfaces with at least one concrete implementation (Dependency Inversion).\nThe scaffolded project should be copy-ready as a starter template for similar projects.\n\nIMPLEMENTATION & INTEGRATION TESTING (after scaffold):\nAfter scaffolding, the workflow continues with full implementation and integration testing:\n1. /architecture-review-full reviews the built foundation (architecture-review + architecture-scalability-review + production-readiness-review → one consolidated Architecture Health Report); fix ALL BLOCKED/WARN findings BEFORE /plan-execute so every feature builds on a reviewed, verified foundation. Honors the scale-tier guard — ADVISES right-sized structure, never forces heavyweight architecture onto a small T0/B0 project. Then the reference-doc set is ESTABLISHED before any feature code: /scan --target=ui-system, /scan --target=backend-patterns, /scan --target=integration-tests, and /scan --target=project-structure DERIVE the full {REF_DOCS_ROOT} set (design-system + scss-styling-guide + frontend/backend patterns + integration-test + project-structure) FROM the reviewed-and-fixed foundation + the Phase-03 golden-path examples, so downstream feature work reads accurate references from day one; scaffold has already hand-authored {REF_DOCS_ROOT}/ui-review-principles.md (the UI-review PRINCIPLES the /ui-review gate reads). Skip /scan --target=ui-system when the project has no UI stack (log the reason); the other three always apply.\n2. /why-review validates design rationale before coding. It shares the `reference-docs-and-rationale` all-return barrier with the step-1 /scan set: FIRST launch this /why-review as a fresh read-only `code-reviewer` sub-agent in FULL mode; its inputs are PLAN₂, the reviewed foundation source, and the /architecture-review-full report — it does not read, wait on, or regenerate the {REF_DOCS_ROOT}/* docs the in-flight scans derive, and a missing or stale reference doc there is expected, recorded as NOT VERIFIABLE, and never a trigger for /scan or /project-init (it returns any Trade-Off Interrogation questions unanswered; the orchestrator asks them via AskUserQuestion after the barrier and never self-approves a one-way door), THEN run the four /scan steps INLINE in order — the barrier holds one spawned sub-agent plus this inline scan chain; each scan fans out its own sub-agents from the orchestrator, so a scan is never dispatched as a sub-agent, and the four scans write disjoint reference docs. Advance to /plan-execute only after ALL members return. If the why-review FAILs and forces a foundation change, fix it and re-run every /scan whose reference doc covers the changed area before /plan-execute.\n3. /plan-execute implements the feature (backend + frontend)\n4. Domain-entity DDD quality review is owned by the nested /workflow-review-changes occurrence (conditional on domain entity files changing); it detects anemic models, missing invariants, and VO misclassification.\n5. /spec [mode=tests] writes test specifications (feature doc Section 8)\n6. /artifact-review --type=spec-tests validates spec coverage and correctness\n7. Third /plan + /plan-review cycle plans integration test architecture\n8. /integration-test generates integration tests from specs\n9. Integration-test coverage review is owned by the nested /workflow-review-changes occurrence\n10. /integration-test-verify verifies the configured integration command\n11. /e2e-test runs only when docs/project-config.json → e2eTesting is configured; otherwise records evidence-backed N/A\n12. /test runs final full/focused verification and reports exact results and exit status\n13. /workflow-review-changes for quality (run the canonical 19-step sequence from .claude/workflows.json: changes-review + whole-target why-review in parallel, the specialist review batch in parallel, code-simplifier, plan/plan-review/plan-execute, and the conditional step-14 whole-target why-review)\n13b. /workflow-e2e --source=context is REQUIRED near the end and owns configured E2E plus default-on screenshot evidence; record N/A/ENVIRONMENT-BLOCKED when unavailable.\n14. Production-readiness security review is owned by the nested /workflow-review-changes occurrence\n15. Final /test, then /watzup to close (the conditional /scan --target=domain-entities → /docs-update refresh is owned by the nested /workflow-review-changes occurrence)\nThis ensures greenfield projects ship with integration test coverage from day one.\nSCALE-TECHNIQUE GATE (advisory): during the tech-stack-research, architecture-design, architecture-scalability-review, and production-readiness-review steps, apply SYNC:scale-technique-gate — derive the target scale tier from evidence (T0 internal / T1 <10k / T2 10k–1M / T3 millions+), judge which system-design techniques (rate limiting, caching, load balancing, queues, sharding, autoscaling, CI/CD, observability, DR, etc.) that tier WARRANTS, and record the Technique Applicability Matrix (each warranted technique judged PRESENT / MISSING-WARRANTED / N/A-by-scale / OVER-ENGINEERED). Advise on warranted-but-missing gaps AND advise AGAINST over-provisioning below tier (do NOT add Kubernetes/sharding/multi-region for a small system). Advisory guidance only — it never changes a score or verdict. Full catalog: .claude/docs/scale-technique-catalog.md.\nSCENARIO-STRESS EVAL (advisory): complementing the scale-technique gate top-down, during the tech-stack-research, architecture-design, architecture-scalability-review, production-readiness-review, and performance-review steps (and whenever solution-architect authors resilience posture), apply SYNC:scenario-stress-eval — REUSE the scale tier already derived by SYNC:scale-technique-gate, derive the orthogonal business-criticality tier (B0 non-critical / B1 important / B2 business-critical / B3 mission-critical or regulated) from evidence, then stress-test the design against concrete scenarios (traffic spike, sustained + data-volume growth, dependency down/slow, node/zone/region loss, data loss/corruption, poison-message/retry storm, cascading/backpressure, cold start, clock skew/duplicate delivery). Record a Scenario Stress Matrix judging each IN-SCOPE scenario WITHSTANDS / DEGRADES-GRACEFULLY / FAILS-HARD / N/A-by-business / OVER-HARDENED with self-heal + trade-off notes. Apply the criticality-signal floor: regulated / PII / financial / health data, money movement, authentication/identity, or legal-compliance scope floors B at B2+ even absent SLA/SLO docs. Right-size in BOTH directions — advise on FAILS-HARD gaps the business warrants AND advise AGAINST OVER-HARDENED resilience a lean B0/B1 system does not need. Advisory guidance only — it never changes a score or verdict. Full catalog: .claude/docs/scenario-stress-catalog.md.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (spec→code→tests→review loop): §8 must derive universally-quantified Invariant/Property TCs (for-ALL-inputs rules + boundary counter-cases) for every [HARD] §4 rule and §5 invariant — not just example scenarios — and back them with property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on changed core-logic = a missing invariant → write the killing test), NOT line-coverage %. Every behavior-changing finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a code-only fix. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-greenfield-init/SKILL.md"
        ]
      },
      "sequence": [
        "idea",
        "web-research",
        "deep-research",
        "market-analysis",
        "business-evaluation",
        "spec-discovery",
        "domain-analysis",
        "why-review",
        "tech-stack-research",
        "architecture-design",
        "architecture-scalability-review",
        {
          "id": "architecture-rationale-review",
          "skill": "why-review"
        },
        "scenario",
        "plan",
        "plan-review",
        {
          "id": "architecture-security-review",
          "skill": "security-review",
          "args": "--report-only"
        },
        {
          "id": "architecture-performance-review",
          "skill": "performance-review",
          "args": "--report-only"
        },
        "plan-review",
        "refine",
        "artifact-review --type=pbi",
        "story",
        "artifact-review --type=story",
        "pbi-challenge",
        "dor-gate",
        "pbi-mockup",
        "plan-validate",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec-clarify",
        "plan",
        "plan-review",
        "scaffold",
        "linter-setup",
        "harness-setup",
        "architecture-review-full",
        "scan --target=ui-system",
        "scan --target=backend-patterns",
        "scan --target=integration-tests",
        "scan --target=project-structure",
        {
          "id": "pre-coding-rationale-review",
          "skill": "why-review"
        },
        "plan-execute",
        "seed-test-data",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "plan",
        "plan-review",
        "integration-test",
        "integration-test-verify",
        "e2e-test",
        "test",
        "workflow-review-changes",
        {
          "id": "greenfield-e2e",
          "skill": "workflow-e2e",
          "args": "--source=context"
        },
        {
          "id": "final-test",
          "skill": "test"
        },
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to start a new project from scratch, init a greenfield project, plan a new application, research and plan before coding, bootstrap a new codebase, build something new"
    },
    "workflow-idea-to-pbi": {
      "description": "PO/BA idea → grooming-ready backlog. TWO modes: (1) SINGLE-PBI DEEP — one concrete idea/ticket/brief → deep single PBI via idea → draft Feature Spec → TDD test specs → domain → plan → PBI/stories → challenge → DoR → mockup → prioritize; (2) MULTI-OPPORTUNITY DISCOVERY — a raw vision/problem → brainstorm (optionally web-research → deep-research) → RICE opportunity map → user multi-select → light per-opportunity PBI loop → cross-PBI ranked backlog. For idea → ONE provisional Feature Spec only (no backlog) use workflow-idea-to-spec.",
      "name": "Idea to PBI",
      "preActions": {
        "injectContext": "LARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, require the complete large_idea_decomposition block in the owning artifacts and carry its stable slice IDs through downstream outputs. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nIDEA TO PBI PROTOCOL:\nCapture and refine a raw idea — or a handed-off artifact/ticket/brief — into a grooming-ready PBI via an idea → test specs → (from those specs) PBI/stories/plan flow, with domain analysis, challenge review, DoR validation, and wireframe. Apply the shared SDD Artifact Contract from shared/sdd-artifact-contract.md in the active skills root and read docs/project-config.json plus {REF_DOCS_ROOT}/docs-index-reference.md for project-specific conventions. Any supported AI tool may produce or review artifacts when this context is synced.\n\nMODE DETECTION GATE (FIRST — pick the track before running any step, then declare it to the user):\n- SINGLE-PBI DEEP MODE — input is ONE concrete idea / ticket / brief. Run the deep single-PBI track: idea → spec-discovery → refine → spec [mode=draft] → spec [mode=tests] → artifact-review --type=spec-tests → spec-clarify → domain-analysis → plan → plan-review → plan-validate → PBI → stories → challenge → DoR → mockup → design-spec → prioritize. SKIP brainstorm, web-research, and deep-research.\n- MULTI-OPPORTUNITY DISCOVERY MODE — input is a raw product vision / problem statement spanning multiple opportunities. Run brainstorm (optionally web-research → deep-research up front to inform framing) → RICE opportunity map → user multi-select → spec-discovery (ONCE, shared across opportunities) → a LIGHT per-opportunity PBI loop (idea → refine → artifact-review --type=pbi → story → artifact-review --type=story → pbi-challenge → dor-gate → pbi-mockup → design-spec), then cross-PBI prioritize. The spec [mode=draft] Feature Spec authoring step, the spec [mode=tests] step, the spec-clarify validation gate, and the plan/plan-review/plan-validate cycle are SINGLE-PBI-DEEP-MODE ONLY — NEVER run them per opportunity (they would multiply N× and stall discovery). domain-analysis runs ONCE up front (shared across opportunities), not per opportunity.\nWhen the input is ambiguous (single concrete ask vs broad vision), ask via AskUserQuestion before step 1.\n\nMANDATORY IMPORTANT MUST ATTENTION RULES:\n1. Each step must invoke its Skill tool — never batch-complete or skip steps\n2. artifact-review is CONDITIONAL — skip if no existing artifact; proceed straight to refine\n3. why-review runs after refine and after domain-analysis. The standalone gates before artifact-review --type=spec-tests and before artifact-review --type=story are omitted because artifact-review runs the same adversarial rationale techniques (steel-man, assumption stress test, pre-mortem, unseen alternatives, contrarian pass) plus the Trade-Off Interrogation Gate on that exact artifact and self-invokes /why-review --validate-findings. The standalone gate after plan-validate is omitted because every plan-review round runs a parallel full-mode /why-review rationale sub-agent over the plan and then /why-review --validate-findings on the merged findings. The standalone gate after artifact-review --type=pbi is omitted because artifact-review --type=pbi (like every review skill) self-invokes /why-review --validate-findings internally as a Findings Validation Gate. Each gate validates WHY before the next artifact step proceeds. FAIL blocks the next artifact step; WARN requires user acknowledgment.\n4. spec [mode=draft] authors the canonical tech-free 8-section Feature Spec §1-7 (idea-sourced, provisional: true, §8 Evidence: TBD) right after refine, then spec [mode=tests] and artifact-review --type=spec-tests run on that draft (BEFORE the PBI is drafted) so the idea is captured as a §1-7 Feature Spec plus testable §8 TC specifications first; spec-clarify (SINGLE-PBI DEEP MODE ONLY) then validates those §8 test-spec decisions with the user — AskUserQuestion on every NON-OBVIOUS / CONFLICTS / high-impact decision — BEFORE domain-analysis and decomposition build on them; domain-analysis and plan/plan-review/plan-validate (grafted from the spec-to-pbi analytical half), then the PBI and stories, are derived FROM those specs (idea → draft Feature Spec → test specs → from those specs to PBI)\n5. pbi-challenge is run by a reviewer different from the drafter — confirm reviewer identity before that step\n6. dor-gate must pass (PASS or WARN) before pbi-mockup is finalized. For UI PBIs, pbi-mockup AND design-spec both run (mockup first, then UI specs) so the PBI carries a faithful HTML mockup that matches the current UI system PLUS tech-agnostic UI specs — both are CONDITIONAL and SKIP for backend-only PBIs (state the skip reason). Both are gated by the existing-UI-research protocol (SYNC:existing-ui-research) so they faithfully match existing screens/components and connected flows. The code-producing design lanes (design --lane=...) are reference-only and are NOT part of this workflow.\n7. Save artifacts at every step to the workflow artifact paths used by the child skills. If artifact roots become configurable later, update the workflow and child skills in the same change.\n8. Write output IMMEDIATELY after each step — never batch\n9. Run docs-update after prioritize and before workflow-end so specs, workflow-feature docs, and TDD/spec docs stay synchronized\n10. Treat AI-generated ideas, PBIs, stories, mockups, and TCs as draft/reference until the owning review or acceptance gate approves them.\n11. DISCOVERY MODE: /brainstorm MUST produce a RICE-scored opportunity map (3–8 items) before any /idea step. The per-opportunity loop (idea → refine → artifact-review --type=pbi → story → artifact-review --type=story → pbi-challenge → dor-gate → pbi-mockup) repeats for EACH selected opportunity — NOT once. spec [mode=draft], spec [mode=tests], and the plan/plan-review/plan-validate cycle stay SINGLE-PBI-DEEP-MODE ONLY.\n12. DISCOVERY MODE: /prioritize at the end is cross-PBI — it ranks ALL PBIs from this session together. This workflow produces a BACKLOG only (no implementation) — hand off to workflow-feature or workflow-big-feature to build the top-ranked PBI.\n13. PRIORITY PROPAGATION & VISIBILITY (MANDATORY, both modes): /prioritize MUST write the computed rank/priority back into every generated PBI's frontmatter `priority` field — never leave a generated PBI without priority. The pbi-mockup generated files AND the feature-presentation deck MUST display each PBI's priority info (rank + RICE/MoSCoW band) — the mockup in its header, the deck in its Scope & backlog slide. This workflow's spec→pbi decomposition half is IDENTICAL to workflow-spec-to-pbi (idea-to-pbi == idea-to-spec + spec-to-pbi).\n\nSTEP SELECTION GATE:\nAfter workflow activation, present the full step list and let user deselect irrelevant ones:\n- [x] Brainstorm (brainstorm) — DISCOVERY MODE ONLY: Double Diamond → RICE-scored opportunity map (3–8 items); SKIP in single-PBI deep mode\n- [ ] Market research (web-research) — DISCOVERY MODE, CONDITIONAL: skip for internal tools or well-understood domains\n- [ ] Deep research (deep-research) — DISCOVERY MODE, CONDITIONAL: runs only when web-research ran; deep-dive top sources into an evidence base that feeds brainstorm (skipped automatically when web-research is skipped)\n- [x] Idea capture (idea) — REPEATS per selected opportunity in discovery mode\n- [x] Spec discovery (spec-discovery) — investigate existing Feature Specs + related code BEFORE drafting PBIs so an already-spec'd capability is never duplicated; surfaces related/affected specs + gaps; runs ONCE up front (shared across opportunities in discovery mode). CONDITIONAL: short-circuits on an empty corpus\n- [ ] Review existing artifact (artifact-review) — CONDITIONAL: only if PO artifact/ticket exists\n- [x] Refine to PBI (refine) — hypothesis, AC, RICE, GIVEN/WHEN/THEN; REPEATS per opportunity in discovery mode\n- [x] Refinement rationale review (why-review) — after refine\n- [x] Feature Spec draft (spec [mode=draft]) — SINGLE-PBI DEEP MODE ONLY: author the canonical tech-free 8-section Feature Spec §1-7 (idea-sourced, provisional: true, §8 Evidence: TBD) BEFORE the §8 test specs\n- [x] Test specifications (spec [mode=tests]) — SINGLE-PBI DEEP MODE ONLY: generate TCs FROM the refined idea (idea → draft Feature Spec → specs)\n- [x] Test specification review (artifact-review --type=spec-tests) — deep mode\n- [x] Spec validation (spec-clarify) — SINGLE-PBI DEEP MODE ONLY: validate the §8 test-spec decisions with the user (TEST-SPEC context) before domain-analysis; the light per-opportunity discovery loop EXCLUDES it (would multiply N×)\n- [x] Domain analysis (domain-analysis) — CONDITIONAL: skip if no new/changed entities; in discovery mode runs ONCE up front (shared)\n- [x] Domain rationale review (why-review) — after domain-analysis\n- [x] Implementation plan (plan) — SINGLE-PBI DEEP MODE ONLY\n- [x] Plan review (plan-review) — deep mode\n- [x] Plan validation (plan-validate) — deep mode\n- [x] PBI review (artifact-review --type=pbi)\n- [x] User stories (story)\n- [x] Story review (artifact-review --type=story)\n- [x] Dev BA PIC challenge (pbi-challenge)\n- [x] Definition of Ready gate (dor-gate)\n- [x] PBI mockup/wireframe (pbi-mockup) — CONDITIONAL: skip for backend-only PBIs; the mockup MUST faithfully match the current UI system (gated by existing-UI-research) AND link back to the governing Feature Spec §6 interaction surface (View Inventory / Key UI States / per-story click-path) so the mockup is traceable to §6\n- [x] UI design spec (design-spec) — CONDITIONAL: skip for backend-only PBIs; authors the PBI's tech-agnostic UI specs right after the mockup so every UI PBI carries BOTH a faithful mockup AND UI specs (both gated by SYNC:existing-ui-research), and the design-spec's View Inventory / Key UI States / per-story click-path must link back to (and stay consistent with) the governing Feature Spec §6 interaction surface\n- [x] Backlog prioritization (prioritize)\n- [x] Documentation synchronization (docs-update) — near-final sync for specs, workflow-feature docs, and TDD/spec docs\n- [x] Stakeholder presentation (feature-presentation) — synthesize all session ideas/specs/PBIs/stories/design-specs/mockups into ONE standalone HTML slide deck for PO/BA/Dev/QC; embeds each existing -mockup.html via <iframe srcdoc> (never regenerated); gap-fills missing PBIs/mockups via workflow-spec-to-pbi / pbi-mockup run as sub-agents\n\nWHY-REVIEW GATES (repeated, purpose-specific):\nRun in sequence after refine and after domain-analysis (artifact-review --type=spec-tests / --type=story own the test-spec and story rationale; no standalone gate follows plan-validate because every plan-review round runs its own parallel /why-review rationale sub-agent; artifact-review --type=pbi self-invokes /why-review --validate-findings internally as a Findings Validation Gate). Challenge the active artifact rationale before the next artifact step:\n- Is this the right next artifact/solution to the stated problem? What was rejected and why?\n- Are the acceptance criteria, story, or TC constraints justified? What breaks if they change?\n- Pre-mortem: if this PBI ships and fails in 3 months, what breaks?\n- Are there simpler alternatives the team has not considered?\nOutput: Why-Review checklist with PASS/WARN/FAIL + adversarial analysis section.\nFAIL blocks the next artifact step — active artifact must be revised first.\n\nTDD-SPEC GATE (after refine + spec [mode=draft], BEFORE the PBI is drafted):\nWith the canonical §1-7 Feature Spec drafted (spec [mode=draft]), map the refined idea’s acceptance criteria into §8 TC specifications up front, so the PBI, stories, and plan are derived FROM the draft Feature Spec + its test specs:\n- Each material acceptance criterion should map to at least one TC ID\n- Route planned TC IDs to Feature doc Section 8 through /spec [mode=tests]; /docs-update later verifies workflow-feature docs and §8 TC ↔ integration test code sync\n- Cover happy path, validation failure, authorization/permission, and important edge cases where applicable\n- Review specs with artifact-review --type=spec-tests before pbi-challenge so reviewers evaluate a testable PBI\n- AI-generated TC drafts are reference-only until review and DoR gates accept them.\n\nSPEC-CLARIFY VALIDATION GATE (SINGLE-PBI DEEP MODE ONLY — after artifact-review --type=spec-tests, before domain-analysis — BLOCKING AskUserQuestion):\nOnce the §8 test specs are authored and reviewed, spec-clarify validates the decisions encoded in them WITH the user before any domain/plan/decomposition work builds on them. Context = TEST-SPEC (a refined idea + drafted §1-7 Feature Spec + §8 TCs): it walks the applicable validation categories (business rules/invariants the §8 set assumes, test-case coverage gaps, cross-spec conflicts vs the spec-discovery landscape), classifies each surfaced decision OBVIOUS / NON-OBVIOUS / CONFLICTS, and asks the user (AskUserQuestion, ≤4 options per call, recommended first, multiple calls as needed) to confirm every NON-OBVIOUS + CONFLICTS + high-impact decision. Confirmed answers are written back into the refined idea / §8 + a Decisions Log; residual <80%-confidence items become Open Questions. NEVER runs in MULTI-OPPORTUNITY DISCOVERY MODE (the light per-opportunity loop excludes it). Spec Validation: questions=3-6 (TEST-SPEC context budget — ask ≥3 only when ≥3 genuine decisions surface; never invent filler to hit the minimum).\n\nMULTI-OPPORTUNITY DISCOVERY LOOP (DISCOVERY MODE core mechanic — folded in from product discovery):\nThe /brainstorm step produces a RICE-scored opportunity map — typically 3–8 opportunities. Present it to the user (AskUserQuestion, multiSelect: true): 'Which opportunities should we develop into PBIs?'. Run the OPPORTUNITY-MAP WHY-REVIEW gate (below) BEFORE the loop. Then for EACH selected opportunity, run this LIGHT loop (9 steps — NO spec [mode=draft], NO spec [mode=tests], NO spec-clarify, NO plan/plan-review/plan-validate; domain-analysis already ran once up front):\n  1. /idea — capture as a structured artifact\n  2. /refine — PBI with hypothesis, AC, RICE, GIVEN/WHEN/THEN\n  3. /artifact-review --type=pbi — BA quality check\n  4. /story — user stories per PBI\n  5. /artifact-review --type=story — story quality check\n  6. /pbi-challenge — Dev BA PIC review (reviewer ≠ drafter)\n  7. /dor-gate — INVEST + DoR pass/fail\n  8. /pbi-mockup — wireframe, faithfully matching the current UI system (SKIP for backend-only PBIs)\n  9. /design-spec — tech-agnostic UI specs for the PBI (SKIP for backend-only PBIs)\nAfter ALL opportunities are processed: run /prioritize across all PBIs (cross-PBI).\n\nTASK DECOMPOSITION GATE (DISCOVERY MODE): After the user selects opportunities, call TaskCreate for EVERY task (N opportunities × 9 loop steps = N×9 tasks min) BEFORE processing any opportunity — do NOT start the loop without a complete task list.\n\nSCALE MANAGEMENT (DISCOVERY MODE): For 6+ selected opportunities, spawn one sub-agent per opportunity (each gets brainstorm context + its task list); the main context runs /prioritize at the end. After every 3 opportunities, update the session summary table.\n\nBRAINSTORM STEP REQUIREMENTS (DISCOVERY MODE):\n- Detect scenario: problem-solving vs new product vs enhancement\n- Apply Double Diamond: problem framing (5 Whys/HMW/JTBD) → opportunity framing (OST/Lean Canvas) → ideation (SCAMPER/Crazy 8s) → convergence (RICE/Kano/2×2)\n- Output: opportunity map with 3–8 RICE-scored items, documented in {PLANS_ROOT}/{plan-dir}/brainstorm-opportunity-map.md\n\nOPPORTUNITY-MAP WHY-REVIEW GATE (DISCOVERY MODE — after brainstorm, before the per-opportunity loop):\nBefore committing to the loop, validate the opportunity map rationale:\n- Are the top-ranked opportunities truly the right problems to solve? What was deprioritized and why?\n- Are RICE scores well-founded or speculative? Challenge Reach and Impact estimates.\n- Pre-mortem: if these opportunities are built and miss in 6 months, what was the root cause?\n- Are there systemic alternatives (platform/process change) that make these opportunities unnecessary?\nOutput: Why-Review checklist with PASS/WARN/FAIL per opportunity. FAIL on a high-ranked opportunity → remove from selection or revisit brainstorm framing; WARN → document risk and proceed with user acknowledgment.\n\nCROSS-PBI PRIORITIZE (DISCOVERY MODE):\n- Aggregate ALL PBIs produced this session; apply cross-PBI RICE + a dependency graph\n- Produce a sprint-ready ranked backlog; flag Must/Should/Could-Have per release scope\n- Output to the configured backlog artifact root\n\nHANDOFF:\nAt workflow-end, AI MUST ATTENTION present:\n- Summary: single-PBI deep mode → 1 PBI created (test specs created/reviewed, plan, DoR result); discovery mode → N PBIs created, X passed DoR, Y need rework, ranked backlog produced; docs sync completed; any blocking items\n- Stakeholder deck: ONE standalone HTML slide presentation at {TEAM_ARTIFACTS_ROOT}/presentations/{YYMMDD}-presentation-{slug}.html synthesizing every PBI/story/spec + embedded mockups for PO/BA/Dev/QC review\n- Recommended next workflow: /start-workflow workflow-feature or /start-workflow workflow-big-feature (implement the PBI / top-ranked PBI from the backlog)\n- Any DoR failures: list specific blocking criteria that must be resolved\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.",
        "readFiles": [
          ".claude/skills/workflow-idea-to-pbi/SKILL.md"
        ]
      },
      "sequence": [
        "web-research",
        "deep-research",
        "brainstorm",
        "idea",
        "spec-discovery",
        "artifact-review",
        "refine",
        "why-review",
        "spec [mode=draft]",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec-clarify",
        "scenario",
        "domain-analysis",
        "why-review",
        "plan",
        "plan-review",
        "plan-validate",
        "artifact-review --type=pbi",
        "story",
        "artifact-review --type=story",
        "pbi-challenge",
        "dor-gate",
        "pbi-mockup",
        "design-spec",
        "prioritize",
        "docs-update",
        "feature-presentation",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "PO/BA wants a grooming-ready PBI backlog from an idea. SINGLE-PBI DEEP: a raw idea — or a handed-off artifact/ticket/brief — through to ONE grooming-ready PBI with a provisional Feature Spec, user stories, TDD test specifications, Dev BA PIC challenge, DoR validation, wireframes, and prioritization. MULTI-OPPORTUNITY DISCOVERY: a raw product vision/problem statement → structured brainstorm → RICE opportunity map → user multi-select → multiple PBIs (light per-opportunity loop) → cross-PBI ranked backlog. For idea → ONE provisional Feature Spec only (no backlog), use workflow-idea-to-spec"
    },
    "workflow-idea-to-spec": {
      "description": "Idea-to-spec: turn a raw idea, vision, or problem into one reviewed provisional canonical spec under the active artifact contract. Stop at the spec; chain workflow-spec-to-pbi for a backlog. Portable README/eight-section/TC details apply only when neither a native profile nor local artifact contract applies.",
      "name": "Idea to Feature Spec",
      "preActions": {
        "injectContext": "CANONICAL SPEC PROFILE (MANDATORY):\nRead docs/project-config.json, the configured feature/spec template, local spec-system-reference.md, and spec-principles.md. Use specRoots.business.path and workflowPatterns.featureDocTemplate for canonical paths; use the native profile/local contract for section roles, IDs, ownership, provisional state, and test/evidence carriers. Apply tech-agnostic prose to designated intent roles; preserve permitted technical contracts in their declared roles.\nThe portable {SPEC_ROOT}/{Bucket}/README.{Feature}.md, tech-free eight-section with mandatory inline §5 Mermaid ERD and §6.2–§6.5 for UI-bearing features, and Section 8 TC-{FEATURE}-{NNN} case format apply only when neither a native profile nor local artifact contract exists. Default cases carry Business Intent / Invariant Guarded, user-visible GIVEN/WHEN/THEN, Evidence, CoveredBy, and status; before code lands, use Evidence: TBD, Status: Planned, and provisional: true. Do not invent a section, ID, provisional field, or second case registry. Reconcile provisional cases with real code and executable evidence only after code exists. Unknown mappings or required coverage are BLOCKED/UNKNOWN, never PASS or NOT-APPLICABLE; invalid contracts never silently fall back.\nLARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, require the complete block in the canonical spec's profile/local-reference-declared role that permits slice plans, then carry its stable slice IDs through downstream outputs. Stop and resolve the mapping only if neither the native profile nor local artifact reference declares a role that permits slice plans; never invent a section. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nIDEA-TO-SPEC PROTOCOL (SPEC-DRIVEN, STOPS AT SPEC):\nConverts a raw idea / product vision / problem statement into ONE canonical provisional project spec. This workflow ENDS at the reviewed spec; it does NOT decompose into PBIs, stories, or a backlog. If the user wants a backlog, hand off to workflow-spec-to-pbi after this workflow completes.\n\nMANDATORY IMPORTANT MUST ATTENTION RULES:\n1. Each idea-framing stage (brainstorm, spec-discovery scope gate, domain-analysis, why-review) requires AskUserQuestion validation before proceeding; the spec-clarify clarification gate likewise requires AskUserQuestion confirmation of every non-obvious decision before the spec is finalized.\n2. Save ALL artifacts to configured artifact and plan roots at EVERY step — write IMMEDIATELY after each task, never batch.\nIf multiple distinct capabilities emerge, confirm scope with the user and author one canonical project spec per capability (sub-agent per capability for 4+).\n4. SPEC-DRIVEN MANDATE: author one canonical project spec via /spec [mode=draft] using the active artifact contract; use the portable tech-free eight-section default only when neither a native profile nor local artifact contract applies. Then author contract-defined provisional test/evidence cases through /spec [mode=tests]. NEVER skip the spec.\n5. PROVISIONAL OUTPUT: because no code exists yet, use the provisional state and planned-case/evidence convention defined by the active native profile or local artifact contract. The first workflow-code-to-spec / spec [mode=update] run against real code reconciles planned cases with implementation and executable proof; clear provisional status only when the acceptance rule defined by the active native profile or local artifact contract is met. Evidence: TBD, Status: Planned, and provisional: true are fallback fields only.\n6. NO BACKLOG: this workflow produces the Feature Spec (plus, for UI ideas, its tech-agnostic UI design specs via /design-spec) only — no PBI/story/DoR/mockup/prioritize. /design-spec authors UI SPECS ONLY (no HTML mockup, no backlog) so the spec-only contract holds; the mockup + full backlog belong to workflow-spec-to-pbi (chain it next) or workflow-idea-to-pbi (idea → full backlog one-shot).\n\nSTEP SELECTION GATE:\nAfter workflow activation, auto-select the applicable steps and skip irrelevant conditional steps. Default step set:\n- [ ] Market & domain research (web-research) — CONDITIONAL, AUTO-SKIP for internal tools or well-understood domains: discover existing products / competitors / market solutions for this idea + the common best-practice patterns, so the brainstorm is informed by what already exists\n- [ ] Deep research (deep-research) — CONDITIONAL, runs only when web-research ran: deep-dive the top sources into an evidence base that FEEDS the brainstorm (skipped automatically when web-research is skipped)\n- [x] Brainstorm — Double Diamond: problem frame, HMW, SCAMPER, converge on the capability to spec\n- [x] Spec discovery (spec-discovery) — investigate existing Feature Specs + related code BEFORE authoring: surface related/overlapping/affected specs, missing features/test-cases/user-stories, system unknowns, and the invariant landscape, then a scope-decision gate (NEW standalone spec / EXTEND an existing spec / SPLIT into N). CONDITIONAL: auto-short-circuits on an empty corpus (greenfield — no specs and no code yet)\n- [x] Domain Analysis (domain-analysis) — CONDITIONAL: skip if no new domain entities involved\n- [x] Why-Review (why-review) — validate the idea framing is the right problem before authoring the spec\n- [x] Idea capture (idea) — capture the converged idea as a structured artifact\nCanonical spec authoring (spec [mode=draft]) — use the native template, roles, identifiers, and provisional-state convention; the portable tech-free eight-section shape applies only when neither a native profile nor local artifact contract applies.\nNative test/evidence case authoring (spec [mode=tests]) — use the active artifact contract's carrier and provisional convention; Section 8 TC-{FEATURE}-{NNN} applies only when neither a native profile nor local artifact contract applies.\n- [x] Test-spec review (artifact-review --type=spec-tests)\nCanonical spec review (artifact-review) — quality-check all profile-required roles and evidence.\nUI design spec (design-spec) — CONDITIONAL, UI ideas only: author tech-agnostic UI specs (NO mockup/backlog; spec-only contract preserved) and couple them to the native interaction-intent owner when declared; use the default Section 6 coupling only when neither a native profile nor local artifact contract defines interaction intent. Keep SYNC:existing-ui-research. SKIP for backend-only ideas.\n- [x] Spec clarification (spec-clarify) — review the authored spec vs the discovered system, brainstorm open questions, audit every hypothesis/decision (OBVIOUS / NON-OBVIOUS / CONFLICTS), and ask the user (AskUserQuestion) to confirm every non-obvious decision before the spec is finalized\n- [x] Why-Review (why-review) — validate the authored spec's rationale + completeness\nDocs sync (docs-update) — sync the canonical spec, configured test/evidence carrier, and only declared derived indexes.\n- [x] Stakeholder presentation (feature-presentation) — synthesize the authored Feature Spec(s) + design-specs into ONE standalone HTML slide deck for PO/BA/Dev/QC; SPEC-ONLY: design-spec ASCII wireframes + inventory/states/tokens tables, NO HTML mockups (preserves the spec-only contract)\n\nSPEC AUTHORING FLOW (core mechanic — idea → provisional Feature Spec):\n  1. (CONDITIONAL) Run /web-research to discover existing products, competitors, market solutions, and common best-practice patterns for this idea — AUTO-SKIP for internal tools / well-understood domains (record the skip reason).\n  2. (CONDITIONAL) Run /deep-research to deep-dive the top sources from step 1 into an evidence base — runs only when step 1 ran; its findings FEED the brainstorm.\n  3. Run /brainstorm to frame the idea (informed by the research evidence when present) and converge on the capability to spec.\n  4. Run /spec-discovery to investigate the surrounding system BEFORE authoring — Glob {SPEC_ROOT}/** to classify every related/overlapping/affected Feature Spec, /investigate + graph-trace the related code logic, then surface gaps (missing features/test-cases/user-stories), system unknowns, and the existing invariant landscape the idea must respect. Ends in a BLOCKING scope-decision gate (NEW standalone spec / EXTEND existing spec X via /spec [mode=update] / SPLIT into N). CONDITIONAL: on an empty corpus (no specs + no code) it records the reason and short-circuits.\n  5. Run /domain-analysis if new domain entities are implied (skip otherwise).\n  6. Run /why-review to validate the idea framing (right problem? pre-mortem? systemic alternatives?).\n  7. Run /idea to capture the converged idea as a structured artifact → configured idea artifact root.\n8. Run /spec [mode=draft] to author the canonical spec using its native template, section roles, and provisional-state convention from the idea plus the spec-discovery landscape. The portable tech-free eight-section Feature Spec and {SPEC_ROOT}/{Bucket}/README.{Feature}.md path apply only when neither a native profile nor local artifact contract defines another format or path.\n9. Run /spec [mode=tests] to author planned behavioral/test-evidence cases in the active artifact contract's carrier before code exists. Section 8 TC-{FEATURE}-{NNN}, Evidence: TBD, and Status: Planned apply only when neither a native profile nor local artifact contract defines another carrier or fields.\n  10. Run /artifact-review --type=spec-tests — test-spec quality check.\n  11. Run /artifact-review — Feature Spec quality check.\n  12. (CONDITIONAL — UI ideas only) Run /design-spec to author the idea's tech-agnostic UI specs — UI-spec author ONLY: NO mockup, NO PBI/backlog (the spec-only contract is preserved). Gated by the existing-UI-research protocol (SYNC:existing-ui-research) so the specs faithfully match the current UI system's existing screens/components and connected flows. SKIP for backend-only ideas (record the skip reason).\n13. Run /spec-clarify (AUTHORED-SPEC context) to validate every applicable native-contract-defined role: scope and boundaries, actors and permissions, business rules/invariants, data/lifecycle/state, process/error flows, acceptance completeness, case/evidence coverage, cross-spec conflicts, and non-functional constraints. Classify each decision OBVIOUS / NON-OBVIOUS / CONFLICTS, brainstorm open questions, then ask the user to confirm every NON-OBVIOUS, CONFLICTS, and high-impact decision. Write confirmed answers into native canonical roles and its declared decision record; loop through /spec [mode=update] for material gaps.\n  14. Run /why-review — validate the authored spec's rationale and completeness.\n15. Run /docs-update to sync the canonical spec, configured test/evidence carriers, and declared derived indexes.\n  16. Run /feature-presentation to synthesize the authored Feature Spec(s) + design-specs into ONE standalone HTML slide deck for PO/BA/Dev/QC (spec-only: design-spec ASCII visuals + inventory/states/tokens tables, NO HTML mockups — the spec-only contract holds). Output → {TEAM_ARTIFACTS_ROOT}/presentations/{YYMMDD}-presentation-{slug}.html.\n\nBRAINSTORM STEP REQUIREMENTS:\n- Detect scenario: problem-solving vs new product vs enhancement\n- Apply Double Diamond: problem framing (5 Whys/HMW/JTBD) → opportunity framing (OST/Lean Canvas) → ideation (SCAMPER/Crazy 8s) → convergence (pick the capability to spec)\n- Output: the converged capability (or a short list, if multiple capabilities — confirm scope with the user)\n- Document in {PLANS_ROOT}/{plan-dir}/brainstorm-idea-frame.md\n\nHANDOFF:\nAt workflow-end, AI MUST ATTENTION present:\nSummary: M canonical specs authored (provisional), native-contract-defined case/evidence coverage, open questions (confidence < 80%)\nCanonical specs authored: list the configured canonical spec paths under the business root; use README paths only when neither a native profile nor local artifact contract defines another canonical path.\nProvisional note: use the native provisional/test-evidence convention until code lands; reconcile via workflow-code-to-spec / spec [mode=update] once implemented. Portable Evidence: TBD and provisional: true fields apply only when neither a native profile nor local artifact contract defines alternatives.\n- Stakeholder deck: ONE standalone HTML slide presentation at {TEAM_ARTIFACTS_ROOT}/presentations/{YYMMDD}-presentation-{slug}.html synthesizing the Feature Spec(s) + design-spec visuals for PO/BA/Dev/QC review\n- Recommended next workflow: /start-workflow workflow-spec-to-pbi (decompose the Feature Spec(s) into a grooming-ready PBI backlog) OR /start-workflow workflow-feature (implement directly from the spec)\n\nAUTO-SKIP RULES:\n- web-research / deep-research: CONDITIONAL — AUTO-SKIP for internal tools / well-understood domains where external market/competitor/best-practice evidence would not change the spec; when run, web-research feeds deep-research which feeds the brainstorm. deep-research is skipped automatically whenever web-research is skipped. Always record the skip reason\nspec-discovery: NEVER skip when any canonical spec or code exists; it is the pre-spec landscape investigation. CONDITIONAL auto-short-circuit ONLY on a truly empty corpus under the configured business root and no code, recorded with a reason.\n- spec-clarify: NEVER skip — the user-confirmation gate is the whole point; when zero non-obvious decisions surface it still records the OBVIOUS decisions + verdict and proceeds\nspec [mode=draft] / spec [mode=tests]: NEVER skip; canonical spec authoring and required case/evidence coverage are the spec-driven core of this workflow.\n- domain-analysis: skip if no new entities/aggregates — ask: 'Does this idea involve new domain entities?'\n\nWHY-REVIEW GATE (after domain-analysis, before spec authoring):\nBefore committing to authoring the spec, validate the idea framing:\n- Is this truly the right problem to solve? What was deprioritized and why?\n- Pre-mortem: if this is built and misses in 6 months, what was the root cause?\n- Are there systemic alternatives (e.g., platform change, process change) that make this unnecessary?\nOutput: Why-Review checklist with PASS/WARN/FAIL.\nFAIL → revisit brainstorm framing before authoring. WARN → document risk and proceed with user acknowledgment.\n\nSPEC-DISCOVERY SCOPE-DECISION GATE (after spec-discovery, before domain-analysis — BLOCKING AskUserQuestion):\nBefore any spec is authored, spec-discovery presents the landscape and asks the user to choose the spec's scope so a duplicate/overlapping spec is never written:\n- (a) NEW standalone Feature Spec — nothing existing covers this capability.\n- (b) EXTEND existing spec X — reroute to /spec [mode=update] on the named spec instead of authoring a new one.\n- (c) SPLIT into N specs — the idea spans multiple capabilities/buckets.\nAlso confirm WHICH existing specs the author must cross-reference. On an empty corpus this gate is skipped (recorded reason).\n\nSPEC-CLARIFY CLARIFICATION GATE (after artifact-review, before final why-review — BLOCKING AskUserQuestion):\nAfter the spec is authored and quality-checked, spec-clarify runs in AUTHORED-SPEC context using the complete active artifact contract, not assumed section numbers. It examines scope, actors/permissions, invariants, data/lifecycle, process/error flows, acceptance, test/evidence coverage, cross-spec conflicts, and non-functional constraints; classifies decisions OBVIOUS / NON-OBVIOUS / CONFLICTS; then invokes AskUserQuestion to confirm every non-obvious, conflicting, and high-impact decision (at most four options per call, recommended first, multiple calls as needed). Preserve the 5–10 question budget only when that many genuine decisions surface; never invent filler. Write confirmed answers to native roles and the declared decision record; residual confidence below 80% stays an Open Question; material gaps loop through /spec [mode=update]. Section references and TC rigor apply only when neither a native profile nor local artifact contract applies.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.",
        "readFiles": [
          ".claude/skills/workflow-idea-to-spec/SKILL.md"
        ]
      },
      "sequence": [
        "web-research",
        "deep-research",
        "brainstorm",
        "spec-discovery",
        "scenario",
        "domain-analysis",
        "why-review",
        "idea",
        "spec [mode=draft]",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "artifact-review",
        "design-spec",
        "spec-clarify",
        "why-review",
        "docs-update",
        "feature-presentation",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "PO/BA wants to turn a raw product idea, vision, or problem into one reviewed provisional canonical spec under the configured root and active artifact contract, without creating a PBI backlog."
    },
    "workflow-refactor": {
      "description": "Code improvement and restructuring workflow with search-first approach",
      "name": "Code Refactoring",
      "preActions": {
        "injectContext": "Role: Refactoring Specialist\nREFACTORING PROTOCOL:\n⚠️ PROJECT CONTEXT: Read docs/project-config.json → workflowPatterns for project-specific architecture patterns, code hierarchy, and naming conventions.\n⚠️ MANDATORY: Search existing code BEFORE planning\n1. Investigate: Find similar refactoring patterns and target architecture examples, then validate with 3+ codebase examples (NOT generic framework docs)\n3. Plan: Identify code smells, define target architecture following discovered project patterns\n4. Validate plan  --  ensure no behavioral changes, only structural\n5. Design rationale is reviewed inside /plan-review (every round runs a parallel full-mode /why-review rationale sub-agent beside its core pass, then /why-review --validate-findings on the merged findings) — no standalone why-review step follows plan-validate\n6. Implement incrementally  --  small, verifiable steps\n7. Verify test specs still match after refactoring with /spec [mode=tests]. Review with /artifact-review --type=spec-tests. Sync Feature Spec §8 ↔ test code with /spec [mode=sync].\n8. Verify/update integration tests with /integration-test — ensures tests reflect refactored code paths.\n9. Simplify: Remove dead code, flatten nesting, extract duplicates\n   CRITICAL: Before removing any code:\n   - Use /investigate skill for 'unused' code verification\n   - Require evidence: grep results + confidence ≥80% + cross-module/service validation\n   - See Investigation Protocol (CLAUDE.md)\n10. Code review: Verify no functional regressions\n11. Run tests  --  all existing tests MUST ATTENTION pass\n12. Summary report of structural improvements\n\nGUARDRAILS:\n- Refactoring MUST ATTENTION NOT change observable behavior\n- Follow project patterns from docs/project-config.json → workflowPatterns (architecture, code hierarchy, naming)\n- Apply project code responsibility hierarchy from docs/project-config.json → workflowPatterns.codeHierarchy\n- Provide file:line evidence of pattern search in plan\n\nPERFORMANCE-SDD ROUTE: If this refactor is performance-driven (query optimization, caching, reducing allocations, improving throughput), run /performance-review for benchmark evidence while preserving observable behavior. Do not use performance/refactor scope to bypass spec, test, or docs sync when behavior, public contract, SLA, performance constraint, state timing boundary, or docs/spec boundary changes. Pure behavior-preserving optimization may skip new TC/integration-test generation only with explicit skip reason and invariant-preservation evidence. /test remains mandatory.\nMANDATORY REFACTOR INVARIANT SAFETY GATES:\n- Preserve existing intent/invariants; refactor MUST NOT change observable behavior unless explicitly approved.\n- STATE MACHINE DATA ASSERT (MOST IMPORTANT MANDATORY ASSERT): for lifecycle/state-machine logic, tests MUST assert persisted transitions and invalid-transition rejection.\n- Before /workflow-end, maintain three-way sync: spec docs ↔ TDD docs ↔ test code via /spec [mode=tests] + /artifact-review --type=spec-tests + /spec [mode=sync] + /integration-test + /integration-test-review + /integration-test-verify + /docs-update. Performance-driven refactors may delegate measurement to /performance-review, but observable behavior preservation and required spec/test/docs sync remain closure gates.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (spec→refactor→tests→review loop): §8 must carry universally-quantified Invariant/Property TCs (for-ALL-inputs rules + boundary counter-cases) for every [HARD] §4 rule and §5 invariant the refactor must PRESERVE — not just example scenarios — backed by property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on the restructured core-logic = a preserved invariant left unguarded → write the killing test), NOT line-coverage %. Every behavior-affecting finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a code-only change. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-refactor/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "plan",
        "plan-review",
        "plan-validate",
        "plan-execute",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec [mode=sync]",
        "integration-test",
        "integration-test-verify",
        "workflow-review-changes",
        "test",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to restructure, reorganize, clean up, or improve existing code without changing behavior; technical debt"
    },
    "workflow-research": {
      "description": "Research & Synthesis: gather web sources on a topic, then synthesize into one of four artifacts selected by --output — cited knowledge report (synthesis), business/market viability evaluation (business-eval), marketing strategy (marketing), or structured course material (course)",
      "name": "Research & Synthesis",
      "preActions": {
        "injectContext": "RESEARCH & SYNTHESIS PROTOCOL:\nThe canonical entry point is the /workflow-research skill — it dispatches to one of four synthesis sequences via --output={synthesis|business-eval|marketing|course}. The Sequence below is the DEFAULT (--output=synthesis); for the other three intents keep the shared research scaffold (web-research → deep-research → … → knowledge-review → workflow-end) and swap ONLY the terminal synthesis skill(s) per the OUTPUT DISPATCH table.\n\nOUTPUT DISPATCH (select by intent BEFORE creating tasks; default synthesis):\n- synthesis (knowledge report): /web-research → /deep-research → /knowledge-synthesis → /knowledge-review → /workflow-end\n- business-eval (business/market evaluation): /web-research → /deep-research → /market-analysis → /business-evaluation → /knowledge-review → /workflow-end\n- marketing (marketing strategy): /web-research → /deep-research → /market-analysis → /strategy-builder → /knowledge-review → /workflow-end\n- course (course material): /web-research → /deep-research → /course-builder → /knowledge-review → /workflow-end\n\nRULES:\n- Detect the target artifact from the prompt and pick the matching --output BEFORE creating tasks; if ambiguous, default to synthesis and state the assumption.\n- Create the TaskCreate plan from the SELECTED --output sequence (not the default) when it differs.\n- Each step MUST ATTENTION invoke its Skill tool — marking a task completed without skill invocation is a workflow violation.\n- Keep claims evidence-based with cited sources; confidence >80% to assert.\n- This workflow produces research artifacts only — no code implementation.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.",
        "readFiles": [
          ".claude/skills/workflow-research/SKILL.md"
        ]
      },
      "defaultMode": "synthesis",
      "variants": {
        "synthesis": {
          "sequence": [
            {
              "id": "synthesis-web-research",
              "skill": "web-research"
            },
            {
              "id": "synthesis-deep-research",
              "skill": "deep-research"
            },
            {
              "id": "synthesis-knowledge",
              "skill": "knowledge-synthesis"
            },
            {
              "id": "synthesis-review",
              "skill": "knowledge-review"
            },
            {
              "id": "synthesis-end",
              "skill": "workflow-end"
            }
          ]
        },
        "business-eval": {
          "sequence": [
            {
              "id": "business-web-research",
              "skill": "web-research"
            },
            {
              "id": "business-deep-research",
              "skill": "deep-research"
            },
            {
              "id": "business-market-analysis",
              "skill": "market-analysis"
            },
            {
              "id": "business-evaluation",
              "skill": "business-evaluation"
            },
            {
              "id": "business-review",
              "skill": "knowledge-review"
            },
            {
              "id": "business-end",
              "skill": "workflow-end"
            }
          ]
        },
        "marketing": {
          "sequence": [
            {
              "id": "marketing-web-research",
              "skill": "web-research"
            },
            {
              "id": "marketing-deep-research",
              "skill": "deep-research"
            },
            {
              "id": "marketing-market-analysis",
              "skill": "market-analysis"
            },
            {
              "id": "marketing-strategy",
              "skill": "strategy-builder"
            },
            {
              "id": "marketing-review",
              "skill": "knowledge-review"
            },
            {
              "id": "marketing-end",
              "skill": "workflow-end"
            }
          ]
        },
        "course": {
          "sequence": [
            {
              "id": "course-web-research",
              "skill": "web-research"
            },
            {
              "id": "course-deep-research",
              "skill": "deep-research"
            },
            {
              "id": "course-builder",
              "skill": "course-builder"
            },
            {
              "id": "course-review",
              "skill": "knowledge-review"
            },
            {
              "id": "course-end",
              "skill": "workflow-end"
            }
          ]
        }
      },
      "sequence": [
        "web-research",
        "deep-research",
        "knowledge-synthesis",
        "knowledge-review",
        "workflow-end"
      ],
      "whenToUse": "User wants to research a topic from web sources and synthesize the findings into a deliverable — a cited knowledge report, a business/market viability evaluation, a marketing strategy, or structured course material"
    },
    "workflow-review-changes": {
      "description": "Review uncommitted changes, conditionally exercise affected observable surfaces, plan and fix blocking findings, then re-review recursively until the current severity bar is clear",
      "name": "Review Current Changes",
      "parallelGroups": [
        {
          "id": "initial-reviews",
          "members": [
            "initial-changes-review",
            "why-review --target=whole-review-target"
          ],
          "conditionalMembers": [],
          "barrier": true
        },
        {
          "id": "reviewers",
          "members": [
            "architecture-review",
            "domain-entities-review",
            "performance-review",
            "integration-test-review",
            "security-review",
            "production-readiness-review",
            "ui-review"
          ],
          "conditionalMembers": [
            "domain-entities-review",
            "ui-review"
          ],
          "barrier": true
        }
      ],
      "preActions": {
        "domainEntityReferenceRefresh": "Immediately before docs-update, run scan --target=domain-entities when the final diff changes entity/model, DTO/contract, persistence schema/migration, or documented entity-sync evidence; otherwise record a cited skip reason.",
        "injectContext": "PRE-COMMIT REVIEW (RECURSIVE):\n\nSEVERITY + ROUND-BAR POLICY (MANDATORY):\n- Classify each validated finding by shipped consequence, not effort: CRITICAL = immediate material risk (security/authority bypass, secrets or PII exposure, irreversible destructive action, data loss/corruption, silent critical-path failure, or failed binary gate); HIGH = material supported-path correctness, contract, invariant, privacy, authority, compatibility, or likely-harm risk; MEDIUM = bounded consequential edge/resilience/observability/testability/maintainability or local architecture gap; LOW = polish or minor documentation/convention/defensive cleanup with no credible present correctness, security, privacy, authority, availability, or data-integrity impact.\n- Round 1 blocks on every validated severity. Rounds 2-3 block only on validated CRITICAL/HIGH/MEDIUM; LOW-only rounds end immediately and list LOWs as deferred. Never re-tier a finding to obtain a pass.\n- Binary gates (tests, security must-fix, required artifacts, generated parity, policy compliance) remain blocking at every round and are never relabeled LOW. Cite file:line evidence, consequence, exposure, reversibility/detectability, and confidence for every finding.\n- For each finding, record the affected asset/user/data/contract/gate, shipped consequence, exposure/likelihood, reversibility/detectability, evidence location, and confidence before applying the round predicate. `NOT VERIFIABLE` is pending evidence, not a severity and never a LOW escape hatch; unresolved material behavior, security, privacy, authority, availability, data-integrity, or binary-gate claims remain open until resolved or explicitly owner-accepted with residual risk.\n\n[BLOCKING] INITIAL PARALLEL PHASE — launch `why-review --target=whole-review-target` (step 2) as a fresh read-only `code-reviewer` sub-agent, then immediately run `changes-review` (step 1) INLINE while it is active. Advance only after BOTH return.\n- Step 1 (`changes-review`) establishes the dimensional baseline: surface analysis by configured project stack and changed-surface categories, review mode, integration-test sync gaps, multilingual translation gaps, spec drift, and internal UI review.\n- Step 2 (`why-review --target=whole-review-target`) independently runs FULL mode over the WHOLE review target + current changes. It consumes no step-1 output and validates its own findings before returning.\n- The SPECIALIST PARALLEL BATCH (`architecture-review`, `domain-entities-review`, `performance-review`, `integration-test-review`, `security-review`, `production-readiness-review`, `ui-review`) MUST be spawned together in a single message via specialized `Agent` tool calls (`architect`, `code-reviewer`, `performance-optimizer`, `integration-tester`, `security-auditor`, `code-reviewer`, `ui-ux-designer`). They are read-only and independent — no shared mutable state, no ordering dependency between them. `production-readiness-review` runs here in READ-ONLY findings/score mode (SRE 12-criteria /24 + Extended SRE Readiness gate) like every other batch member — its standalone 'Validated Fix + Full Re-Review' fixer loop is DEFERRED to the workflow's mutating steps (`code-simplifier` / `plan-execute`), same standalone-vs-batch duality as `security-review`.\n- The UI/frontend quality gate (`/ui-review`) runs in TWO places by design (keep both): (a) INTERNALLY inside `changes-review` (step 1), which invokes it as its UI dimension whenever the diff contains frontend files; AND (b) as a DEDICATED CONDITIONAL member of the parallel batch (`ui-review`, dispatched via the `ui-ux-designer` sub-agent). Both are gated on the same trigger — frontend files present in the diff — so both are SKIPPED entirely when no frontend files changed.\n- `ui-review` is a CONDITIONAL member of the batch: include it ONLY when the diff contains files matching the project's configured frontend/UI file patterns. Skip it entirely (do not spawn it) when no frontend files changed.\n- `domain-entities-review` is a CONDITIONAL member of the batch: include it ONLY when domain entity files changed. Skip it entirely (do not spawn it) when its trigger files are absent.\n- NEVER start the specialist batch before the steps 1–2 initial barrier clears. NEVER serialize the batch (burns 50K+ tokens absorbing inline reports). NEVER start `code-simplifier` until ALL spawned sub-agents return — code-simplifier modifies code and must operate on the consolidated review snapshot.\n- After the parallel specialist batch returns: TaskUpdate the batch steps to completed, read all sub-agent reports, synthesize Critical/High/Medium/Low findings into a consolidation summary, then proceed to `code-simplifier` sequentially.\n\n- Review all staged and unstaged changes\n- Check for: security issues, debug artifacts (console.log, debugger), incomplete code, style violations\n- Verify no sensitive files (.env, credentials) are staged\n- Check architecture compliance, naming, patterns\n- DOMAIN ENTITY REVIEW: If domain entity files in changeset (Domain/, Entities/, ValueObjects/ directories), run /domain-entities-review to check DDD quality (anemic model, VO immutability, invariant enforcement). Skip entirely if no entity files changed.\n- UI/FRONTEND REVIEW: Runs in TWO places (keep both) when the changeset contains files matching the project's configured frontend/UI file patterns: (a) INTERNALLY — step 1 (`changes-review`) invokes /ui-review as its UI dimension; AND (b) as a DEDICATED CONDITIONAL parallel-batch member (`ui-review` via the ui-ux-designer sub-agent). Both check long-content overflow (wrap vs ellipsis+tooltip), responsive multi-screen via flex, flex-vs-fixed sizing (prefer min/max + flex-grow over fixed px), z-index scale discipline (no raw numbers, no !important), project-selected styling conventions, and async UI states (loading/error/empty). Skip both entirely if no frontend files changed.\n- Report findings with file:line references\n- Output: PASS at the current round bar (safe to continue) or ISSUES FOUND (with validated severity, evidence, and deferred LOW list)\n- If ISSUES FOUND: classify each by the active severity policy, plan fixes only for validated blocking findings (CRITICAL/HIGH/MEDIUM, plus every round-1 LOW), review and sanity-check the fix plan, implement fixes, then run the conditional HOLISTIC why-review (step 14)\n- RECURSIVE (CONDITIONAL, INLINE): Step 14 re-runs `why-review` INLINE in the main session over the settled whole target — but ONLY if `plan-execute` actually changed files. If `plan-execute` applied no file changes, skip step 14 (no post-fix state to re-review). When it runs, loop plan -> plan-execute -> why-review until one complete full-mode pass clears the current round bar (round 1: zero findings; round 2+: zero CRITICAL/HIGH/MEDIUM, with LOWs deferred); stop only when the same validated blocker repeats 2 full invocations with no progress.\n- HOLISTIC WHY-REVIEW (step 14, CONDITIONAL — only when the fix cycle changed files, FULL MODE, INLINE): run `why-review` STANDALONE in FULL mode (NOT --validate-findings) over the WHOLE review target combined with the current changes — the complete changeset and the surrounding code/spec/docs it touches reviewed as one artifact, with the full adversarial rationale gate. This is a DIFFERENT lens from the dimensional reviewers and from the step-2 startup whole-target pass (which reviewed the pre-fix state): it catches design-rationale, hidden-coupling, easy-to-change and whole-package gaps the per-file/per-dimension passes miss. If it surfaces findings, re-enter the plan -> plan-execute -> why-review fix loop, re-running step 14 until a complete full-mode why-review pass clears the current round bar (round 1: zero findings; round 2+: zero CRITICAL/HIGH/MEDIUM, with LOWs deferred; max re-do / 3-repeat-blocker escalation per why-review's own goal gate). Only then proceed to docs-update (step 17). This step is the reason the workflow catches what a standalone why-review of the target would catch but the dimensional batch alone did not.\n- LOGIC REVIEW: Verify changes match their stated intention. Trace business logic paths. Clean code can be wrong code.\n- BUG DETECTION: Check for null safety, boundary conditions, resource leaks, concurrency issues per bug-detection-protocol.\n- TEST SPEC VERIFICATION: Cross-reference changes against TC-{FEATURE}-{NNN} test specifications. Flag untested code paths.\n- INTEGRATION TEST SYNC: Identify changed business logic files (handlers, services, controllers, commands, queries, resolvers — infer from project conventions). For each, verify a corresponding test file exists. If missing, surface to user via AskUserQuestion — mandatory, not advisory.\n- MULTILINGUAL UI SYNC CHECK: If UI-facing files changed and project localization is multilingual (`localization.enabled` + `supportedLocales.length > 1`), verify translation file updates. If missing, surface via AskUserQuestion — mandatory, not advisory.\n- DOC SYNC DEFERRAL: DO NOT update Feature Specs or test spec TCs during review steps. The dedicated docs-update step handles all of this: /spec (§1-7 Feature Spec) + /spec [mode=tests] (§8 test spec update) + /spec [mode=sync] (§8 TCs ↔ test code) + optional /spec-index [mode=index] (derived bucket INDEX/ERD refresh). TEST SPEC VERIFICATION above is READ-ONLY cross-reference only — flag gaps, do not write.\nMANDATORY REVIEW-CHANGES GATES:\n- SPEC/TDD/TEST THREE-WAY SYNC is blocking: changed behavior must match specs + TCs + test code.\n- SPEC DRIFT ADJUDICATION (apply SYNC:spec-drift-adjudication): for every behavior-changing file, do NOT silently flag a one-directional 'stale doc'. Adjudicate per shared/sdd-artifact-contract.md Drift Gates whether the divergence is CODE-WRONG (change violates an intended spec rule/AC/invariant -> BLOCKING finding, fix code/test against intended behavior) or SPEC-STALE (intentional new behavior the spec no longer reflects -> run /spec [mode=update] FIRST, then /spec [mode=tests] + /spec [mode=sync]); AMBIGUOUS -> AskUserQuestion before editing either side. Never normalize drift just because code/tests are green. Unadjudicated behavior-vs-spec divergence is a blocking finding.\n- STATE MACHINE DATA ASSERT (MOST IMPORTANT MANDATORY ASSERT): for lifecycle/state-transition changes, verify persisted-state assertions and invalid-transition rejection tests.\n- Missing or stale docs/tests are blocking findings; route fixes through /spec [mode=tests] + /artifact-review --type=spec-tests + /integration-test + /integration-test-review + /integration-test-verify + /spec [mode=sync] + /docs-update.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (review = the spec→code→tests loop's closing pass): the test-quality bar for changed core-logic is the MUTATION-SCORE gate (Gate 1 — a surviving mutant on a changed line = a missing invariant → write the killing test), NOT line-coverage %; flag any [HARD] §4 rule or §5 invariant lacking a universally-quantified Invariant/Property TC (for-ALL-inputs + boundary counter-case). Every behavior-changing finding MUST emit a Dual-Feedback Ledger row that feeds BOTH the spec AND the tests — a blank Spec-feedback OR Test-feedback cell that survives to the fix phase = the review is INCOMPLETE, never a code-only fix. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete review pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-review-changes/SKILL.md"
        ]
      },
      "sequence": [
        {
          "id": "initial-changes-review",
          "skill": "changes-review"
        },
        "why-review --target=whole-review-target",
        "architecture-review",
        "domain-entities-review",
        "performance-review",
        "integration-test-review",
        "security-review",
        "production-readiness-review",
        "ui-review",
        "code-simplifier",
        "plan",
        "plan-review",
        "plan-execute",
        {
          "id": "why-review",
          "skill": "why-review",
          "applicability": {
            "when": "The fix cycle ran and /plan-execute changed files — validated blocking findings were fixed, so the settled post-fix whole target must be re-reviewed and proven clean.",
            "skipReason": "No validated blocking findings required fixes (/plan-execute changed no files), so there is no post-fix state to re-review; record the skip with the clean review evidence."
          }
        },
        {
          "id": "experience-review",
          "skill": "experience-review",
          "applicability": {
            "when": "The current target includes a configured experienceVerification surface whose reviewOn trigger intersects the diff, or the diff creates or changes a likely user-facing or externally observable surface; when the latter is true without usable configuration, run the skill and record ENVIRONMENT-BLOCKED.",
            "skipReason": "After inspecting docs/project-config.json and the diff, no configured or likely observable surface is in scope; record NOT-APPLICABLE with evidence citing the inspected paths."
          }
        },
        "scan --target=domain-entities",
        "docs-update",
        {
          "id": "review-changes-end",
          "skill": "workflow-end",
          "applicability": {
            "when": "This workflow is the TOP-LEVEL invocation — the user ran /start-workflow workflow-review-changes, or /changes-review routed here directly.",
            "skipReason": "Nested inside a parent workflow (workflow-feature, workflow-bugfix, workflow-refactor, workflow-big-feature, workflow-greenfield-init, workflow-spec-sync, workflow-code-to-spec [update]); the parent owns /workflow-end and its own terminal wrap-up. Return control to the parent after docs-update instead of announcing workflow completion or running the /understand + Next-Steps handoff."
          }
        },
        {
          "id": "review-changes-watzup",
          "skill": "watzup",
          "applicability": {
            "when": "This workflow is the TOP-LEVEL invocation — the user ran /start-workflow workflow-review-changes, or /changes-review routed here directly.",
            "skipReason": "Nested inside a parent workflow (workflow-feature, workflow-bugfix, workflow-refactor, workflow-big-feature, workflow-greenfield-init, workflow-spec-sync, workflow-code-to-spec [update]); the parent owns /workflow-end and its own terminal wrap-up. Return control to the parent after docs-update instead of announcing workflow completion or running the /understand + Next-Steps handoff."
          }
        }
      ],
      "stepMeta": {
        "initial-changes-review": {
          "executionMode": "inline"
        },
        "why-review --target=whole-review-target": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "why-review": {
          "executionMode": "inline"
        },
        "architecture-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "domain-entities-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "performance-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "integration-test-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "security-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "production-readiness-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "ui-review": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "code-simplifier": {
          "executionMode": "inline"
        },
        "experience-review": {
          "executionMode": "inline",
          "contextBudget": "medium"
        },
        "docs-update": {
          "executionMode": "subagent",
          "contextBudget": "high"
        }
      },
      "whenToUse": "User wants to review current uncommitted, staged, or unstaged changes before committing"
    },
    "workflow-architecture-audit": {
      "description": "Architecture Audit — audit the whole project's architecture, scalability, and production readiness in one pass; synthesize one consolidated Architecture Health Report with three sub-scores and one combined verdict. Read-only: fixes route to a follow-up plan/feature workflow.",
      "name": "Architecture Audit",
      "preActions": {
        "injectContext": "ARCHITECTURE AUDIT (READ-ONLY, ONE PASS, PROGRESSIVE SYNTHESIS):\n\n- This workflow audits the WHOLE project architecture + scalability + production readiness in one pass and synthesizes ONE consolidated Architecture Health Report that grows from IN PROGRESS to FINISHED.\n- The core step `architecture-review-full` runs INLINE in the main session because it SPAWNS sub-agents. It: (1) resolves scope (whole project / current diff / specific path); (2) fans out the three deliberately-non-overlapping reviewers as PARALLEL read-only sub-agents in ONE message behind an all-return barrier — `architecture-scalability-review` (architect, scorecard /20), `architecture-review` (architect, 13-category PASS/WARN/BLOCKED), `production-readiness-review` (code-reviewer, SRE /24 + 8-item gate); (3) PROGRESSIVE SYNTHESIS — creates the ONE consolidated report file at status `IN PROGRESS` when fan-out starts and MERGES each child's findings into it AS that child returns (never held in memory to the end), DEDUPING the siblings' intentional cross-references (one underlying issue = one finding citing every source); (4) FIX-REPORT-PER-REVIEW `/why-review` gate — a SINGLE merged `/why-review` pass that WALKS EACH of the three review faces plus the dedup and fixes the consolidated report in place (revise severities, remove false positives, restore any distinct issue the dedup collapsed); (5) FINALIZE — lock the combined verdict (worst-case rollup: any BLOCKED / NOT READY / HIGH RISK dominates) and flip the report status to `FINISHED`.\n- Parallelism lives INSIDE `architecture-review-full` (it owns the fan-out + all-return barrier), so this workflow declares NO workflow-level parallel groups.\n- The workflow-level `why-review` step is the FINAL VALIDATION GATE over the AUDIT FINDINGS — a distinct altitude from the engine's per-face finding-level fix, and the machine-visible guarantee that no audit finding ships without a why-review pass. EVERY prior step routes its output into this gate, and the gate BOTH validates findings AND reviews results across the steps it can reach: (a) the `investigate` scope map — validate nothing in-scope was missed and nothing out-of-scope pulled in; (b) the FINALIZED consolidated report — verdict-rollup correctness, dedup completeness, cross-review severity consistency, and each of the three review faces' contributions intact. `docs-update` runs AFTER this gate and is NOT validated by it — it self-validates its own doc diff by re-invoking `why-review` inline on non-trivial edits before workflow-end. Each PRIOR step, on completion, MUST hand its findings + results forward to this gate; a prior step is not done until its output has been routed to the final `why-review`. `docs-update`, which runs after the gate, owns validation of its own output.\n- This is a READ-ONLY audit: it produces findings + a verdict only. Every validated finding routes to a FOLLOW-UP `/plan` or feature workflow that owns the fix — no fixes are applied in this workflow.\n- SCALE-TECHNIQUE MATRIX (advisory deliverable): the consolidated Architecture Health Report MUST include the merged Technique Applicability Matrix produced by the three sub-reviewers via SYNC:scale-technique-gate — derive the tier ONCE: architecture-scalability-review's tier is authoritative for the merged matrix, note (do not merge) any divergent child tier — then list each warranted technique as PRESENT / MISSING-WARRANTED / N/A-by-scale / OVER-ENGINEERED with advice. This matrix is ADVISORY guidance only: it is deduped onto one axis and NEVER changes any sub-score, the combined verdict, or a gate result. Full catalog: .claude/docs/scale-technique-catalog.md.\n- SCENARIO-STRESS MATRIX (advisory deliverable): the consolidated Architecture Health Report MUST also include the merged Scenario Stress Matrix produced by the three sub-reviewers via SYNC:scenario-stress-eval — pin ONE authoritative scale tier AND business-criticality read (architecture-scalability-review's derived T-tier + B-tier is canonical; note, do not merge, any divergent child read) — then judge each in-scope scenario (big-traffic / big-data / dependency-failure / node-loss / data-corruption / self-heal survival vs. business need) WITHSTANDS / DEGRADES-GRACEFULLY / FAILS-HARD / N/A-by-business / OVER-HARDENED. Like the technique matrix this is ADVISORY guidance only: deduped onto one axis, it NEVER changes any sub-score, the combined verdict, or a gate result. Full catalog: .claude/docs/scenario-stress-catalog.md.\n- After the final `why-review` gate confirms the report, `docs-update` refreshes any impacted documentation, then `workflow-end` clears state and `watzup` wraps up.",
        "readFiles": [
          ".claude/skills/workflow-architecture-audit/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "architecture-review-full",
        "why-review",
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "stepMeta": {
        "investigate": {
          "executionMode": "subagent",
          "contextBudget": "high"
        },
        "architecture-review-full": {
          "executionMode": "inline"
        },
        "why-review": {
          "executionMode": "inline"
        },
        "docs-update": {
          "executionMode": "subagent",
          "contextBudget": "high"
        }
      },
      "whenToUse": "User wants to review my project architecture, run an architecture health check, check is this production ready, or audit scalability/coupling across the whole project"
    },
    "workflow-code-to-spec": {
      "description": "Code-to-spec: author or maintain one canonical project spec per capability from existing code, keeping the native contract, implementation, and test evidence synchronized. Portable README/eight-section/TC format applies only when neither a native profile nor local artifact contract applies.",
      "name": "Code to Feature Spec",
      "preActions": {
        "injectContext": "SPEC-DRIVEN-DEV PROTOCOL:\nModes: init-full | update | audit.\nBefore authoring, read docs/project-config.json, the configured template, local spec-system-reference.md and spec-principles.md, and specArtifacts when declared. Use specRoots.business.path and workflowPatterns.featureDocTemplate for the canonical path; use specArtifacts and local references for section roles, identifiers, ownership, and test/evidence carriers. Apply tech-agnostic prose only to designated intent roles; preserve permitted technical detail in contract/evidence roles. Portable defaults apply only when neither a native profile nor local artifact contract applies: {SPEC_ROOT}/{Bucket}/README.{Feature}.md, tech-free Sections 1–7, mandatory inline §5 Mermaid ERD, §6.2–§6.5 for UI-bearing features, and Section 8 TC-{FEATURE}-{NNN} cases with Business Intent / Invariant Guarded, user-visible GIVEN/WHEN/THEN, Evidence: [Source: namespace/service/id], CoveredBy, and status.\nCode remains the technical source of truth; the canonical spec remains the requirements/behavior source. Maintain one canonical spec per capability and create only project-declared derived aids.\nStep 0: detect init-full/update/audit, map changed code to the documented capability owner, and confirm the target path when ownership is ambiguous.\nScale gate: 4+ capabilities = MUST spawn one spec sub-agent per capability in ONE message.\nInit-full: investigate → plan → plan-review → plan-validate → spec [mode=init] → spec [mode=tests] → artifact-review --type=spec-tests → artifact-review → docs-update → workflow-end. Derive requirements, contracts, acceptance, and cases from source evidence; reconcile every required native role and executing test proof.\nUpdate mode: git diff → impact map → spec [mode=update] for impacted native roles → spec [mode=tests] for changed behavior → artifact-review --type=spec-tests → spec [mode=sync] for configured case/evidence links and declared derived outputs → docs-update → workflow-end.\nUI-INTENT / INTERACTION-SURFACE REFRESH — CONDITIONAL: when changed code carries user-facing behavior, refresh the interaction-intent owner declared by the native profile or local artifact contract and link the governing design-spec where the contract expects it. For the portable fallback, use Section 6 View Inventory, Key UI States, and per-story click path only when neither source defines interaction intent. Backend-only changes state the reason for skipping UI-intent refresh.\nNew PBI/requirement update: run dor-gate when a changed PBI is being made implementation-ready; run pbi-mockup only for UI/journey changes.\nAudit mode: compare canonical spec history with source-code history and report stale capabilities/native roles; do not assume README layout.\nMANDATORY SPEC-DRIVEN SYNC GATES:\n- Three-way sync contract: native intent/contract/acceptance roles ↔ configured cases and evidence ↔ executing test code. Follow local spec-system-reference.md, including its STATE MACHINE DATA ASSERT mandate. Unknown required mapping is BLOCKED/UNKNOWN, never not-applicable or pass.\n- Run docs-update as near-final sync before workflow-end; watzup runs after workflow-end for every mode. Refresh only project-declared derived indexes/catalogs.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: name the protected business intent or invariant and ensure tests fail when it breaks.\n- Spec-Loop Discipline: derive universally quantified property cases, including boundary counter-cases, for every hard invariant in native contract roles; require the mutation-score gate, not line coverage, for protected core logic. Feed uncovered behavior into both canonical spec and executable tests through a dual-feedback ledger; re-review the whole spec/test/code package until no new gap or hidden rule remains.",
        "readFiles": [
          ".claude/skills/workflow-code-to-spec/SKILL.md"
        ]
      },
      "defaultMode": "update",
      "variants": {
        "init-full": {
          "sequence": [
            {
              "id": "init-investigate",
              "skill": "investigate"
            },
            {
              "id": "init-plan",
              "skill": "plan"
            },
            {
              "id": "init-plan-review",
              "skill": "plan-review"
            },
            {
              "id": "init-plan-validate",
              "skill": "plan-validate"
            },
            {
              "id": "init-spec",
              "skill": "spec",
              "args": "[mode=init]"
            },
            {
              "id": "init-spec-tests",
              "skill": "spec",
              "args": "[mode=tests]"
            },
            {
              "id": "init-review-spec-tests",
              "skill": "artifact-review",
              "args": "--type=spec-tests"
            },
            {
              "id": "init-review",
              "skill": "artifact-review"
            },
            {
              "id": "init-docs",
              "skill": "docs-update"
            },
            {
              "id": "init-end",
              "skill": "workflow-end"
            },
            {
              "id": "init-watzup",
              "skill": "watzup"
            }
          ]
        },
        "update": {
          "sequence": [
            {
              "id": "update-review",
              "skill": "workflow-review-changes"
            },
            {
              "id": "update-spec",
              "skill": "spec",
              "args": "[mode=update]"
            },
            {
              "id": "update-spec-tests",
              "skill": "spec",
              "args": "[mode=tests]"
            },
            {
              "id": "update-review-spec-tests",
              "skill": "artifact-review",
              "args": "--type=spec-tests"
            },
            {
              "id": "update-spec-sync",
              "skill": "spec",
              "args": "[mode=sync]"
            },
            {
              "id": "update-docs",
              "skill": "docs-update"
            },
            {
              "id": "update-end",
              "skill": "workflow-end"
            },
            {
              "id": "update-watzup",
              "skill": "watzup"
            }
          ]
        },
        "audit": {
          "sequence": [
            {
              "id": "audit-investigate",
              "skill": "investigate"
            },
            {
              "id": "audit-spec",
              "skill": "spec",
              "args": "[mode=audit]"
            },
            {
              "id": "audit-review",
              "skill": "artifact-review"
            },
            {
              "id": "audit-docs",
              "skill": "docs-update"
            },
            {
              "id": "audit-end",
              "skill": "workflow-end"
            },
            {
              "id": "audit-watzup",
              "skill": "watzup"
            }
          ]
        }
      },
      "sequence": [
        "investigate",
        "plan",
        "plan-review",
        "plan-validate",
        "spec",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "artifact-review",
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "Initialize a canonical spec from existing code, sync it after code changes, audit staleness, or prepare for a migration. Use spec-index alone only for declared derived outputs."
    },
    "workflow-spec-to-pbi": {
      "description": "Generate a complete, dependency-aware PBI backlog from existing canonical specs under the active artifact contract. Audit freshness, preserve native IDs/evidence, decompose into releasable outcomes, create stories and DoR evidence, and rank the backlog.",
      "name": "Spec to PBI Backlog",
      "preActions": {
        "injectContext": "CANONICAL SPEC PROFILE (MANDATORY):\nRead docs/project-config.json, the configured spec template, local spec-system-reference.md, and spec-principles.md. Map native intent, contract, acceptance, lifecycle/permission/data, identifier, UI-intent, and test/evidence roles to PBI outcomes, constraints, citations, and verification needs. Preserve native IDs and carriers; never synthesize a missing numbered section or duplicate case registry.\nPortable {SPEC_ROOT}/{Bucket}/README.{Feature}.md and numbered-section/US-/BR-/TC- mappings below apply only when neither a native profile nor local artifact contract exists. Default cases carry Business Intent / Invariant Guarded, user-visible GIVEN/WHEN/THEN, Evidence, CoveredBy, and status. Unknown role/owner/case mappings are UNKNOWN and block that mapping; do not call them absent or green. The review, user-clarification, releasable-outcome, priority-propagation, docs-sync, and presentation gates remain required in the declared order.\nLARGE-IDEA DECOMPOSITION CHAIN (MANDATORY):\nFor new, greenfield, big, ambiguous, multi-capability, or release-scoped work, evaluate the shared isLargeIdea rule before authoring a spec, PBI, story, or plan. When any signal is true, read the complete block from the canonical spec's profile/local-reference-declared role that permits slice plans and carry its stable slice IDs through downstream outputs. Stop PBI decomposition only if neither the native profile nor local artifact reference declares a role that permits slice plans; resolve that mapping rather than inventing a section or moving the block to a new artifact. Ordinary isolated ideas omit the block and do not create {PRODUCT_ROADMAP_DOC}. Run /scenario conditionally for the embedded scope. A standalone product-roadmap route is explicit-only; an existing supplied roadmap is read-only context.\nRELEASABLE PBI PROTOCOL (MANDATORY): Every generated PBI MUST be one independently releasable actor-facing outcome with a complete entry-to-result journey, visible/persisted truth, applicable access/failure/recovery behavior, scope, and evidence. Technical/foundation/setup/migration work is enabling work attached to a releasable PBI or a separate actor-facing outcome—never a standalone technical PBI. UI PBIs MUST include a complete multi-view mock-app surface: all required pages/views, navigation, common/domain/page components, applicable states, and a connected full-flow demo result; one static/disconnected screen fails. Apply .claude/skills/shared/releasable-pbi-contract.md.\nFor a genuinely isolated brownfield change or bugfix, record Decomposition Applicability: EXEMPT with the reason and accepting owner; do not create {PRODUCT_ROADMAP_DOC}. An explicitly supplied roadmap remains read-only context.\nThe scenario step is conditional for the selected embedded scope; product-roadmap is not an implicit workflow step. Preserve the canonical sequence order after applicability filtering.\n\nSPEC TO PBI BACKLOG PROTOCOL:\nUse when the user has existing canonical specs under the active artifact contract and wants implementable PBIs generated from them. Resolve canonical paths from project configuration and local references; use the README path only when neither declares a canonical path.\n\nMANDATORY RULES:\n1. Treat the native canonical specs as input; do not brainstorm unrelated opportunities. Decompose each PBI from artifact-contract-defined intent, acceptance, contracts, lifecycle/permissions/data, UI-intent, and test/evidence roles. Use §3 US/AC, §4 BR, §5 ERD, §6 flows, §7 permissions, and §8 TC only when neither a native profile nor local artifact contract applies.\n2. Run spec-index audit first when a canonical spec may be stale versus code. Then, after domain-analysis and why-review and BEFORE decomposition, run spec-clarify to validate decisions from native roles with the user. It does not re-author the canonical spec; confirmed material changes route through /spec [mode=update] first.\n3. Build a capability x feature/operation inventory before creating any PBI.\n4. Decompose large canonical specs into independently releasable actor-facing vertical slices. Attach cross-cutting enabling work to a releasable PBI or define a separate actor-facing outcome; never emit a standalone technical/foundation/setup/migration PBI.\n5. For each PBI, include acceptance criteria, story points, dependencies, priority, domain impact, test/evidence needs, and DoR status. Carry native requirement/acceptance/rule/scenario IDs as the primary citation spine and keep declared evidence carriers as secondary traceability.\n6. Run domain-analysis when the spec implies new/changed entities, aggregates, invariants, state machines, or cross-service ownership.\n7. For each UI PBI, run pbi-mockup THEN design-spec (both CONDITIONAL — SKIP for backend-only PBIs with a stated reason; both gated by SYNC:existing-ui-research). pbi-mockup covers the complete releasable multi-view flow; design-spec authors the PBI UI specification. Link both to the interaction-intent owner declared by the native profile or local artifact contract; use the fallback Section 6 surface only when neither defines interaction intent. Preserve parity with workflow-idea-to-pbi.\n8. Run prioritize once at the end across all generated PBIs to produce a dependency-aware ranked backlog. PRIORITY PROPAGATION (MANDATORY): prioritize MUST write the computed rank/priority back into every generated PBI's frontmatter `priority` field — never leave a generated PBI without priority. Every generated PBI carries priority info.\n9. Write artifacts immediately after each capability/feature is processed; never hold all PBIs in memory.\n10. Run docs-update after prioritize and before workflow-end so canonical specs, configured test/evidence carriers, and project-declared derived indexes stay synchronized.\n11. Run feature-presentation after docs-update (before workflow-end) to synthesize all generated PBIs/stories/specs/design-specs/mockups into ONE standalone HTML stakeholder deck for PO/BA/Dev/QC; it embeds each existing -mockup.html via <iframe srcdoc> (never regenerated) and MUST surface each PBI's priority/rank in the Scope & backlog slide.\n12. PRIORITY VISIBILITY (MANDATORY): the pbi-mockup generated files AND the feature-presentation deck MUST display each PBI's priority info (rank + RICE/MoSCoW band) — the mockup in its header, the deck in its Scope & backlog slide.\n\nSCALE GATE:\n- 1-3 capabilities: process inline with task tracking.\n- 4-10 capabilities: split tasks by capability and feature group.\n- 10+ capabilities or very large specs: process incrementally by capability group, maintain a coverage matrix, and stop only when every spec feature is mapped to PBI/Shared Task/Out-of-scope.\n\nSPEC-CLARIFY VALIDATION GATE (after domain-analysis + why-review, before plan / decomposition — BLOCKING AskUserQuestion):\nThe canonical spec is the input. After the spec-index freshness audit and domain-analysis/why-review, spec-clarify validates decomposition-driving decisions with the user before any PBI is built. Treat this as EXISTING-SPEC context: inspect all artifact-contract-defined intent, contract, acceptance, lifecycle/permission, and evidence roles plus cross-spec conflicts; use §1–8 categories only for the portable fallback. Classify decisions OBVIOUS / NON-OBVIOUS / CONFLICTS, and invoke AskUserQuestion to confirm every non-obvious, conflicting, and high-impact decision (at most four options per call, recommended first, multiple calls as needed). The budget is 4–8 questions only when that many genuine decisions surface; for 4+ capabilities sample shared decisions plus each capability within budget; never invent filler. spec-clarify does not re-author the canonical spec: route confirmed material changes through /spec [mode=update] first.\n\nOUTPUTS:\n- {TEAM_ARTIFACTS_ROOT}/pbis/{date}-pbi-{slug}.md for each PBI (each carries a `priority` frontmatter field).\n- {TEAM_ARTIFACTS_ROOT}/pbis/{date}-pbi-{slug}-mockup.html for each UI PBI (header shows priority/rank).\n- {TEAM_ARTIFACTS_ROOT}/design-specs/{date}-designspec-{slug}.md for each UI PBI (tech-agnostic UI specs).\n- {TEAM_ARTIFACTS_ROOT}/backlog/spec-to-pbi-{date}-backlog.md with rank, dependency graph, priority, and recommended order.\n- {TEAM_ARTIFACTS_ROOT}/presentations/{YYMMDD}-presentation-{slug}.html — the stakeholder deck synthesizing all PBIs (with priority/rank), stories, specs, design-specs, and mockups.\n- tmp/reports/spec-to-pbi-{date}-{bucket}.md with source spec coverage and unresolved questions.\ndocs-update report confirming canonical specs, configured test/evidence carriers, and declared derived outputs are synchronized.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.",
        "readFiles": [
          ".claude/skills/workflow-spec-to-pbi/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "spec-index",
        "domain-analysis",
        "why-review",
        "spec-clarify",
        "scenario",
        "plan",
        "plan-review",
        "plan-validate",
        "refine",
        "artifact-review --type=pbi",
        "story",
        "artifact-review --type=story",
        "pbi-challenge",
        "dor-gate",
        "pbi-mockup",
        "design-spec",
        "prioritize",
        "docs-update",
        "feature-presentation",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants a complete prioritized PBI/story backlog from existing canonical specs under the active artifact contract, including capability slicing and implementation ordering."
    },
    "workflow-spec-sync": {
      "description": "Update test specs and feature docs after code changes, bug fixes, or PR reviews",
      "name": "Spec Sync (Post-Change)",
      "preActions": {
        "injectContext": "TEST SPEC UPDATE WORKFLOW:\nUse after code changes, bug fixes, or PR reviews to keep test specs in sync.\nA native contract may be declared by config or local references; resolve the active artifact contract from docs/project-config.json (specArtifacts) and the required local references first, and use the strict portable defaults only when neither config nor local references declares a native artifact contract. An unresolved mapping stays UNKNOWN/BLOCKED.\n1. Review what changed (git diff or PR diff)\n2. Update test specs in the Feature Spec §8 (Test Specifications) using /spec [mode=tests] — §8 is the canonical in-place home; there is no separate dashboard (retired 2026-06-10)\n3. Sync §8 ↔ integration test code via /spec [mode=sync] (forward: §8 TCs → test code)\n3b. UI-INTENT / INTERACTION-SURFACE REFRESH — CONDITIONAL: only when the changed behavior is user-facing (else state the skip reason — backend-only change, no §6 change). When user-facing behavior changed, run /spec (ui-intent intent) alongside the spec [mode=sync] step to refresh the affected Feature Spec §6 interaction surface — View Inventory, Key UI States, and the per-story click-path — and link the governing design-spec so the §6 interaction-surface stays in sync with the changed behavior.\n4. Generate/update integration tests for changed TCs\n5. Run tests to verify\n\nKey: /spec [mode=tests] diffs existing TCs against current code, adds regression TCs for bugfixes.\nMANDATORY TEST-SPEC UPDATE GATES:\n- Treat spec docs + Section 8 as intent/invariant source; do not encode buggy behavior as expected.\n- Three-way sync contract (§8 TCs ↔ test code, including the STATE MACHINE DATA ASSERT mandate for affected lifecycle transitions) is canonical in {REF_DOCS_ROOT}/spec-system-reference.md → Three-Way Sync Triad — follow it exactly.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (sync = the spec→tests loop, re-reviewed to zero): for every changed/added [HARD] §4 rule and §5 invariant, sync a universally-quantified Invariant/Property TC (for-ALL-inputs rule + boundary counter-case) — not just example scenarios — and back it with property/metamorphic tests whose quality bar is the MUTATION-SCORE gate (a surviving mutant on the changed core-logic = a missing invariant → write the killing test), NOT line-coverage %. Every behavior-changing finding feeds the Dual-Feedback Ledger into BOTH the spec AND the tests (a blank Spec-feedback OR Test-feedback cell = INCOMPLETE), never a test-only or spec-only edit. Re-review the whole package (spec + tests + code, not just the diff) and loop until a complete pass surfaces zero new gap or hidden rule — each cycle enriches the spec.",
        "readFiles": [
          ".claude/skills/workflow-spec-sync/SKILL.md"
        ]
      },
      "sequence": [
        "workflow-review-changes",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "spec [mode=sync]",
        "integration-test",
        "integration-test-review",
        "integration-test-verify",
        "test",
        "docs-update",
        "workflow-end"
      ],
      "whenToUse": "After fixing a bug update test specs, after code changes update test specs, after PR review update test specs, sync test specs after changes, update test documentation after implementation"
    },
    "workflow-visualize": {
      "description": "Create visual Excalidraw diagrams from codebase investigation or web research",
      "name": "Visual Diagram",
      "preActions": {
        "injectContext": "VISUAL DIAGRAM PROTOCOL:\nThis workflow creates Excalidraw diagrams. Two paths based on source:\n\nPATH A — Codebase Visualization (default if topic is about this project):\n1. Investigate: Find relevant files, architecture, code patterns, and trace relationships and data flow\n3. Diagram: Generate .excalidraw file visualizing the findings\n\nPATH B — Knowledge Visualization (if topic requires web research):\n1. Web Research: Research the topic broadly (max 10 WebSearch)\n2. Deep Research: Deep-dive into top sources (max 8 WebFetch)\n3. Diagram: Generate .excalidraw file visualizing the synthesized knowledge\n\nGUARDRAILS:\n- Ask user which path (A or B) if ambiguous\n- Output .excalidraw files to docs/diagrams/ (create dir if needed)\n- Use kebab-case filenames describing the diagram subject\n- MUST ATTENTION render and validate diagram (render-view-fix loop)\n- Read references/color-palette.md and references/element-templates.md before generating\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.",
        "readFiles": [
          ".claude/skills/workflow-visualize/SKILL.md"
        ]
      },
      "defaultMode": "codebase",
      "variants": {
        "codebase": {
          "sequence": [
            {
              "id": "codebase-investigate",
              "skill": "investigate"
            },
            {
              "id": "codebase-diagram",
              "skill": "excalidraw-diagram"
            },
            {
              "id": "codebase-end",
              "skill": "workflow-end"
            }
          ]
        },
        "knowledge": {
          "sequence": [
            {
              "id": "knowledge-web-research",
              "skill": "web-research"
            },
            {
              "id": "knowledge-deep-research",
              "skill": "deep-research"
            },
            {
              "id": "knowledge-diagram",
              "skill": "excalidraw-diagram"
            },
            {
              "id": "knowledge-end",
              "skill": "workflow-end"
            }
          ]
        }
      },
      "sequence": [
        "investigate",
        "excalidraw-diagram",
        "workflow-end"
      ],
      "whenToUse": "User wants to visualize, diagram, draw, or create visual representation of workflows, architectures, concepts, systems, or research findings"
    },
    "workflow-seed-test-data": {
      "description": "Generate or enhance test data seeders that simulate QC happy-path scenarios for a feature area. Investigates existing patterns, implements idempotent command-based seeders, reviews compliance, simplifies.",
      "name": "Seed Test Data",
      "preActions": {
        "injectContext": "SEED TEST DATA PROTOCOL:\n⚠️ PROJECT CONTEXT: Read docs/project-config.json → 'Data Seeders' context group for project-specific seeder base class, file location, config keys, and DI registration pattern. Then read {REF_DOCS_ROOT}/seed-test-data-reference.md for the complete project-specific implementation guide.\n\nUNIVERSAL RULES (apply to ALL projects):\n1. Environment gate FIRST — development or config-enabled only. NEVER production.\n2. Command-based ONLY — call application-layer commands. NEVER direct DB/repo for domain entities. Seeder = QC orchestrator.\n3. No duplicate logic — commands own validation + domain rules; seeder provides valid inputs.\n4. Idempotency — check existing count BEFORE seeding; seed only remaining = target - existing.\n5. Count-configurable — read count from project config key (see project-config.json). Loop from existing to target.\n6. Restart-safe — idempotency inherently handles restarts.\n\nPROJECT-SPECIFIC CONTEXT:\n- Read docs/project-config.json → 'Data Seeders' rules for environment gate key, count key, and DI registration.\n- Read {REF_DOCS_ROOT}/seed-test-data-reference.md for implementation template, reference files, and project-specific DI scope rules.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define success criteria before execution; loop until observable checks pass.\n- Tests Verify Intent: when creating or reviewing specs/tests, name the protected business intent or invariant and ensure the test would fail if that intent breaks.\n- Spec-Loop Discipline (seeder tier — tailored): seeders are orchestration, not business logic, so property/metamorphic generation and the MUTATION-SCORE gate are N/A here — do NOT force them. Apply the dual-feedback half: every seeded scenario MUST stay consistent with the §5 invariants (commands own validation; a seeder that produces state violating an invariant is a bug), and any DOMAIN RULE a seeder encodes (a required precondition, a status/relationship the scenario assumes) belongs in the spec — feed it into BOTH the spec (the rule) AND, where that rule is testable, the tests, never a seeder-only fix.",
        "readFiles": [
          ".claude/skills/workflow-seed-test-data/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "seed-test-data",
        {
          "id": "experience-review",
          "skill": "experience-review",
          "applicability": {
            "when": "The seeded data reaches a configured experienceVerification surface whose reviewOn trigger intersects the change, or the seeder creates or changes data a QC/operator actually observes through a running application, API, CLI, or generated artifact; when the latter is true without usable configuration, run the skill and record ENVIRONMENT-BLOCKED.",
            "skipReason": "After inspecting docs/project-config.json and the seeder diff, no configured or likely observable surface consumes the seeded data; record NOT-APPLICABLE with evidence citing the inspected paths."
          }
        },
        "changes-review",
        "code-simplifier",
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "User wants to seed test data, implement data seeders, generate realistic development environment data, add happy-path scenarios for a feature, create dummy data for manual QC testing, fill dev database with realistic test cases"
    },
    "workflow-write-integration-test": {
      "description": "Write or update integration tests for existing code — spec-first: investigate domain logic → write/update specs → generate test code → 7-gate review (incl. change coverage) → run and verify",
      "name": "Write Integration Tests",
      "preActions": {
        "injectContext": "WRITE INTEGRATION TEST PROTOCOL:\n⚠️ PROJECT CONTEXT: Read docs/project-config.json → integrationTestVerify and framework.integrationTestDoc, plus its referenceDocs and relevant existing tests. These select the runner, suite layout, fixtures, data setup, waits, annotations, and repeat policy. Follow strong engineering defaults while adapting them to the project's language, architecture, and current state; never assume .NET, Angular, a database, a handler/command layout, or a specific helper.\n⚠️ TEST-CASE OWNER: Resolve specArtifacts, specRoots, and the project spec/reference docs. Use the configured carrier and identifier mappings when present; otherwise use the framework strict-default spec format when that is the selected project contract. Do not invent §8 sections, TC IDs, CoveredBy fields, or annotations for a custom carrier.\n⚠️ SCOPE: State the target behavior, affected test suites, project-configured command, and applicable quality gates before authoring.\n\n1. Investigate: trace the actual entry point and observable behavior; inspect the invariant/data owner, relevant consumers, and comparable tests. Identify persistence, events/messages, retries, UI/API boundaries, or async effects only when project evidence shows they apply.\n2. Specify intent: update the selected canonical case/spec owner through /spec [mode=tests]. Preserve the project's case format and map each case to its named intent/contract; if the strict default profile applies, use its documented Section 8/TC contract. Include success, boundary, rejection, authorization, or recovery scenarios when the requirement and risk call for them; no arbitrary case minimum.\n3. Review cases: check that scenarios protect distinct requirements/invariants, state their observable outcomes, and match the configured identity/profile. Do not require a case dimension that the product contract does not have.\n4. Integration Test: author in the project's native language, runner, test organization, and assertion style. Use documented fixtures/builders and real entry paths when those paths are part of the behavior under test. Lower-level setup is valid when it establishes preconditions without bypassing the behavior being tested. Await observable outcomes using runner-native waits or an evidenced project helper. Use collision-resistant data only where tests share mutable state. Add case annotations/links only when the project contract defines them; assert the state or result the system owns, not incidental infrastructure bookkeeping.\n5. Integration Test Review: assess assertion value, valid data/setup, determinism/isolation, behavior ownership, configured case traceability, scenario fidelity, and coverage of changed behavior. Map changed behavior to appropriate tests; do not require one test per source file or a spec case when the selected profile has no such carrier. Validate findings, fix only validated blockers, and repeat the full review after fixes.\n6. Integration Test Verify: run the configured full and focused commands, fail invalid/zero-match selections, and report runner output/counts and exit status. Follow integrationTestVerify.guidance and project references for repeat proof; when unspecified, use two fresh green runs without destructive reset for persistent-state suites. Never claim a capability that was not exercised.\n7. Spec Sync: reconcile the configured case carrier and executing tests using its native mapping. Under the strict default profile, update the documented TC/CoveredBy links; under a custom profile, use its declared identifiers and fields. Never fabricate fields for a profile that does not define them.\n8. Docs Update: update project references, feature/spec docs, and derived docs only at their configured owners and only when the changed behavior or conventions make them stale.\n9. Close and summarize with commands, evidence, case/test mapping, and remaining gaps.\n\nGUARDRAIL: Read the relevant production and test source before writing assertions. Wait only for a real precondition/outcome; do not blanket-wrap database assertions in a universal helper.\nGUARDRAIL: Set up realistic valid state through documented fixtures or public paths where that preserves the scenario. Do not bypass the invariant under test or mutate unrelated persistent/shared data.\nGUARDRAIL: On failure, use /debug-investigate before edits and classify test, source, environment, or ambiguity. Fix source at the project-identified invariant owner; never weaken assertions or change tests merely to match broken behavior.\nGUARDRAIL: Never force green by deleting/skipping tests, weakening assertions, widening assertion timeouts, retrying failed assertions, or narrowing the agreed scope.\nUNIVERSAL RULES:\n- Goal-Driven Execution: define observable success criteria before execution; loop until the configured verification evidence passes.\n- Tests Verify Intent: name the protected behavior/technical invariant and assert an owned outcome that fails when the intent breaks.\n- Spec/Test Discipline: retain the project's canonical spec/test-case owner and traceability profile. For universally quantified requirements, add property/metamorphic coverage when the test stack supports it; use mutation testing when configured or warranted by risk. Do not require mutations on every changed line or fabricate spec artifacts when no owner applies.\n- Keep local conventions and architecture in project config/reference docs; these refine the framework defaults without weakening core correctness or safety rules.",
        "readFiles": [
          ".claude/skills/workflow-write-integration-test/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "spec [mode=tests]",
        "artifact-review --type=spec-tests",
        "integration-test",
        "integration-test-review",
        "integration-test-verify",
        "spec [mode=sync]",
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "Write integration tests for a specific command/handler, add test coverage to an untested feature, update integration tests after code changes, integration test authoring from scratch for a feature area, cover uncommitted code changes with integration tests, generate integration tests from existing test specs or feature docs, review/audit existing integration tests for quality, flakiness, traceability, or failures"
    },
    "workflow-integration-test-green": {
      "description": "Drive an integration-test suite to fully green with a bounded convergence loop — verify the whole system (or the named target), adjudicate every failure with debug-investigate + integration-test-review before any edit, fix at the owning layer, re-verify from scratch, then sync spec TCs, the integration-test reference doc, and feature docs",
      "name": "Integration Test Green (Verify · Adjudicate · Fix · Loop)",
      "preActions": {
        "injectContext": "INTEGRATION TEST GREEN PROTOCOL (Verify → Adjudicate → Fix → Re-verify, looped):\n⚠️ PROJECT CONTEXT: Read docs/project-config.json → integrationTestVerify, including guidance, quickRunCommand, testProjectPattern/testProjects, systemCheckCommand, startupScript, and referenceDocs, plus framework.integrationTestDoc. These and current tests select runner, scope, fixture/data rules, timeouts, and repeat policy. Never hardcode a language-specific runner, database, helper, or directory layout.\n⚠️ SCOPE DEFAULT: Verify the whole relevant system using the project-configured test roots/selection. With no narrower user target, do not use only the git-changed subset. Resolve and record the concrete test project/suite list; if no applicable suite exists, report evidence-backed N/A.\n\n1. Investigate: resolve the target to the configured suites and inspect the failing path, its invariant owner, relevant setup, and expected observable result.\n2. Integration Test Verify Fix-Loop (/integration-test-verify --fix-loop): set the Goal Contract FIRST, then loop:\n   a. Run the configured full verify pass INLINE over a fixed, explicit scope. Follow integrationTestVerify.guidance for repeat evidence; when unspecified, require two fresh green runs without destructive reset for persistent-state suites. Report actual runner counts/summary and exit status.\n   b. On failure, adjudicate BEFORE edits with /debug-investigate and REPORT-ONLY /integration-test-review. Trace source ownership through project architecture/reference/code evidence; never assume Entity → Service → Handler or any other fixed layer order.\n   c. Emit one Fault Verdict per failure: TEST-WRONG | TEST-NOT-OPTIMAL | SOURCE-WRONG | ENVIRONMENT-BLOCKED | AMBIGUOUS, with evidence and confidence. AMBIGUOUS behavior requires clarification; an environment fault follows documented setup/recovery or remains BLOCKED.\n   d. /fix the validated fault at the component that owns the violated invariant or test contract. SOURCE-WRONG keeps or strengthens the test that caught it. TEST-NOT-OPTIMAL repairs scenario/setup fidelity without weakening the expected outcome.\n   e. On every round with a fix, run INLINE report-only /changes-review over that round's fix diff, validate findings with /why-review --validate-findings, and fold validated findings into the same round. No fix means skip with a recorded reason; unresolved validated findings block convergence.\n   f. Round Integrity Check: executed coverage must not shrink, skipped coverage must not grow, and the agreed suite scope must remain fixed.\n   g. Re-run a fresh full verify using the same configured scope and policy. Keep the loop bounded; escalating blockers include non-shrinking/increasing failures, lost coverage, environment blockage, exhausted attempts, or open validated review findings.\n3. Spec Sync (/spec [mode=sync]): reconcile executing tests with the configured specArtifacts/test-case profile. If the strict default profile applies, sync its Section 8 TC/CoveredBy contract; if a custom profile applies, use its declared carrier and fields. Do not fabricate strict-default fields for custom profiles.\n4. Reference Scan: refresh the configured integration-test reference only when the suite or its conventions changed; preserve its configured path/owner.\n5. Docs Update: update other configured canonical/derived docs that became stale, including feature evidence where applicable.\n6. Close the workflow and summarize the scope, configured command, actual results, fixes/reviews, traceability, and remaining gaps.\n\nGUARDRAIL — NEVER force green: do not weaken/remove assertions, add skips, widen assertion timeouts, retry a failing assertion, mutate unrelated persistent data, or narrow scope. Fix the evidenced scenario or product defect and restart verification from a fresh run.\nGUARDRAIL — a suite that appears greener by losing executed coverage REGRESSED; the round integrity check blocks convergence.\nGUARDRAIL — ENVIRONMENT-BLOCKED is not a retry loop: stop, mark BLOCKED, and cite the missing configured prerequisite/recovery path. Never change tests because the system was unavailable.\nGUARDRAIL — no fix ships without code review: every round with a fix uses the configured review gates before final verification.\nMANDATORY INTEGRATION-TEST-GREEN GATES:\n- Set the Goal Contract FIRST. Success requires a fresh full verify for the fixed scope under the project's configured repeat policy, with no test deleted, skipped, weakened, or scope reduced to get there.\n- /integration-test-verify, /debug-investigate, /integration-test-review, and /changes-review run INLINE via the Skill tool so their in-session review/fix gates remain active; their documented internal fan-outs remain intact.\n- /integration-test-review is REPORT-ONLY inside the loop. If it self-fixes, treat that as the round's fix half and do not double-fix.\n- Every pass/fail claim is backed by actual runner output. Report exact counts when the runner exposes them; otherwise report its real summary and exit status.\n- Documentation sync is part of DONE. Sync configured spec/test-case owners, project test references, and other impacted docs after convergence.\nUNIVERSAL RULES:\n- Tests Verify Intent: a test must protect a named business rule/invariant and fail when that intent breaks; never relax it to mirror broken behavior.\n- Test-Failure Fault Adjudication: compare failures against the governing configured spec/case when present and against the source. Without a spec, use the accepted requirement or named expected behavior. If intent is materially ambiguous, stop and ask.",
        "readFiles": [
          ".claude/skills/workflow-integration-test-green/SKILL.md"
        ]
      },
      "sequence": [
        "investigate",
        "integration-test-verify --fix-loop",
        "debug-investigate [on-failure]",
        "fix [on-failure]",
        "spec [mode=sync]",
        "scan --target=integration-tests",
        "docs-update",
        "workflow-end",
        "watzup"
      ],
      "whenToUse": "Make all integration tests pass, fix failing integration tests, drive the integration test suite to green, run the whole integration test suite and fix whatever fails, integration tests are red after a change, verify the whole system integration tests pass repeatably, loop until all integration tests are green, diagnose and fix an intermittent or flaky integration test"
    }
  }
}
