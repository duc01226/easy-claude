---
service: Angular
database: MongoDB
---

# CoveredBy Carrier

The canonical test-coverage carrier is `CoveredBy:`. It must be exempt from the
M1/M2 prose scans exactly as its legacy alias `IntegrationTest:` is.

The payloads below carry banned tech tokens on purpose. An identically-shaped line
under an UNRECOGNISED bold label is flagged, so these lines stay clean only while
both labels are in EVIDENCE_CARRIER_LABELS — which is what makes this fixture
non-vacuous. The tokens are deliberately named ONLY inside the carrier lines.

**CoveredBy:** `Angular RabbitMQ MongoDB`
**IntegrationTest:** `Angular RabbitMQ MongoDB`
