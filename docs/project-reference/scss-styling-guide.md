# SCSS Styling Guide

<!-- Last scanned: 2026-03-15 -->
<!-- This file is referenced by Claude skills and agents for project-specific context. -->

## Status: Not Applicable

**easy-claude** is a Claude Code enhancement framework written entirely in JavaScript (CommonJS). It contains no SCSS, CSS, or any styling files. There are no stylesheets, mixins, variables, or theming patterns to document here.

### Why This File Exists

This placeholder exists so that `/scan-scss-styling` has a known output location when easy-claude is adopted into a project with SCSS. Without it, future AI sessions would search the codebase for `.scss` files that do not exist.

### Related Skills (for Target Projects)

easy-claude includes skill definitions that guide SCSS work in **other** projects — not in this repository:

- **Skills:** `/frontend-design`, `/design-spec`, `/web-design-guidelines`
- **Shared protocols:** `design-system-check.md`, `ui-system-context.md`
- **Scan skill:** `/scan-scss-styling` — run this after adopting easy-claude into a project that uses SCSS to populate this file with mixins, variables, theming conventions, and responsive breakpoints.

### When to Populate

Run `/scan-scss-styling` after easy-claude is integrated into a project that contains `.scss` files. The scan will replace this placeholder with a full SCSS reference.
